# MarketGameSim 项目须知

## 提交前必须本地跑通

```bash
pytest
ruff check .
ruff format --check .
```

CI 的 `lint` job 与 `test` job 是独立的两步，`pytest` 全绿不代表 `ruff` 也会通过——
0.1.1 首次提交时就因为没跑 lint，被 CI 的 `ruff check .` 挡下 105 处违规（多数是
超长行、未清理的 import/变量）。提交前在本地跑一遍，几秒钟能挡住，不用等 CI 跑完
再回来改。

`ruff format .` 与 `ruff check . --fix` 能自动处理大部分问题（超长行、未排序/未使用
的 import 等）；剩下的（未使用变量、过宽的异常断言、废弃写法等）需要手工看一眼再改，
改完重新跑一遍上面三条确认全绿。

## 每次修复必须补充配套的仓库内回归测试

修复一个问题时，必须在同一次提交里为它补上能进入仓库测试套件的回归测试（单元测试
或集成测试），不能只用临时脚本口头验证一下就算完成。`pytest` 全绿不代表修复到位——
它只代表现有测试没被破坏，如果新修复的行为压根没有对应测试，`pytest` 通过和"没修"
在可验证性上是一回事。

0.1.2 里程碑的多轮检视暴露过这个问题：`LIQUIDATION_STALE` 世代校验、`agent_initial_bp`
准入闸口接线、`decision_event_id` 引用错误、做市商维持保证金分支等一系列 P0/P1 级
修复，验证过程都要靠复核者临时写脚本独立复现，而不是仓库里能重复运行的测试——这意味着
这些修复没有任何自动化保护，后续改动完全可能在不知情的情况下把它们悄悄改回去。更严重
的一次是：`T503/KPI-009` 的残差校验曾经被正确地写成 `assert`，后来在没有 git 记录、
没有说明理由的情况下被降级成不影响测试结果的 `warnings.warn`——如果当初有一个断言残差
为 0 的测试锁定这个行为，这次降级会被 CI 直接拦下，而不是要等到下一轮人工复核才发现。

具体要求：
- 新增/修改的行为分支，必须有测试覆盖它的正反两种结果（比如"应该被拒绝的场景"和
  "应该被接受的场景"都要有断言，不能只测其中一边）；
- 已知但暂不修复的缺口，用 `pytest.mark.xfail(strict=True)` 显式标记并写明原因，
  不要留白——这样缺口一旦被意外修复，测试会从 xfail 转为需要人工确认的失败，而不是
  被无声遗忘；
- 涉及多个记录/多个账户同批处理的逻辑（比如强平批量扫描、因果链外键），至少要有一个
  覆盖"多条记录同时存在"的测试用例，不能只测单条记录的场景——这类问题历来最容易在
  单条场景下测试通过、批量场景下才暴露索引错位等 bug；
- 如果某个安全校验的实现方式从"失败即报错/拒绝"改成"失败仅警告/仅记录"，这个变更
  本身必须在提交信息或代码注释里显式说明原因，不能悄悄发生。
