# 0.2.1 H1 验证记录

**日期**：2026-09-04  
**证据类别**：`engineering-demonstration`  
**研究声明**：不适用；交互数据由 evidence guard 排除在正式研究证据之外。

## 验收映射

| 验收项 | 主要自动化证据 | 结果 |
|---|---|---|
| AC-001 | `tests/unit/interactive/test_session.py` | passed |
| AC-002 | `tests/integration/test_interactive_observation.py` | passed |
| AC-003 | `tests/integration/test_human_order_path.py` | passed |
| AC-004 | `test_pacing.py`、`test_interactive_control.py` | passed |
| AC-005 | `tests/integration/test_interactive_replay.py` | passed；双进程摘要一致 |
| AC-006 | `tests/integration/test_interactive_evidence_guard.py` | passed；批量零部分写入 |
| AC-007 | `test_interactive_client.py`、`test_interactive_user_journey.py` | passed |
| AC-008 | `test_interactive_bundle.py`、`test_interactive_delivery.py` | passed |

## 质量门

使用项目内隔离环境执行唯一公开入口：

```bash
PATH="$PWD/.venv/bin:$PATH" .venv/bin/python tools/verify.py
```

结果：合同真源、规格生命周期、pytest、Ruff lint 与 Ruff format 全部通过；pytest 为
`2316 passed, 1 skipped`。跳过项仅因受限沙箱禁止创建 socket；同一客户端测试在允许绑定
`127.0.0.1` 的环境独立执行为 `3 passed`。Windows 的 loopback、重放与路径兼容回归已加入
`.github/workflows/ci.yml` 的 `windows-interactive` job。

## 交付入口

```bash
python -m market_game_sim.interactive.delivery
```

该命令原子生成 `RUN.md`、`manifest.json`、`input-journal.jsonl`、`run.jsonl` 与离线
`replay.html`；输入篡改、截断、幂等键冲突和 artifact 哈希不符均 fail closed。
