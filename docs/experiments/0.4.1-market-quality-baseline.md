# 0.4.1 市场质量首测（成果门 H2-E1 / 退出条件 E6）

**证据级别**：`engineering-demonstration`——不进任何 evidence index，不建立研究声明
（[`0.4.1 spec`](../features/0.4/0.4.1-ai-market-ecology/spec.md) NFR-503）。
**日期**：2026-09-23　**装配**：`DEFAULT_LIVE_ROSTER`（`roster-c812f33d03fe71f7b385bef720563af5`）
——做市商族 6 + 趋势跟随 9 + 均值回归 9 + 情绪噪声 6，共 30 个代理，种子 7。
**复现**：`python -m market_game_sim.metrics.quality_run --seconds 2200`
**口径拥有者**：门限与判定见 spec §6；统计检验见
[`0.1.2 基准市场验证协议`](0.1.2-market-validation-protocol.md) 与 `metrics/validation.py`；
本文只记录实测值，不定义任何口径。

## 1. 为什么有两轮

0.1.2 协议要求单次研究运行至少 **2000 个采样点**（Δt = MD-001 = 1 逻辑秒）。600 逻辑秒
只有 601 个采样点，五项 stylized facts 里四项因此判「不适用」——不是市场不好，是样本
不够。第二轮跑 2200 逻辑秒，让统计检验真正可用。两轮都如实记录。

## 2. 市场质量六项（SC-501）

| 指标 | 门限 | 600 逻辑秒 | 2200 逻辑秒 | 判定 |
|---|---|---|---|---|
| 成交/分钟 | ≥ 30 | 152.8 | 135.6 | PASS |
| 双边盘口可用时间 | ≥ 0.99 | 1.000 | 1.000 | PASS |
| 中位盘口档位/边 | ≥ 5 | 2.0 | 1.0 | **FAIL** |
| 中位有效价差 | ≤ 20 bp | 3.03 | 1.76 | PASS |
| 成交/委托 | ≥ 0.001 | 0.0396 | 0.0338 | PASS |
| 墙钟/逻辑秒 | ≤ 0.5 | 0.127 | **0.549** | **FAIL**（长跑） |

**与方向变更时的实测对照**（[`ADR-011`](../decisions/011-market-engine-trader-layering.md) 背景表，
修复前）：3.0 笔/分钟、47.7% 双边可用、中位档位 (1, 1)、价格振幅 0.000%。该表的数字来自
撤单不发行情的旧代码，且早于冷启动锚与相位修复，**不可与本表直接比较**，此处只作方向参照。

### 两项未通过的原因（不是调门限能解决的）

- **中位档位 1–2 档**：6 个做市商每次各报一侧、价位分散 ±3 tick，每侧约 3 个价位。要到
  5 档需要更多做市商或更大价位分散——属于 Phase 2 的 `H2-E2`（E3）调参，不在 E6 范围。
  2200 秒比 600 秒更低（1.0 vs 2.0），因为长跑中噪声族持仓累积、更多挂单被吃掉。
- **长跑墙钟超预算**：`agent/tape.py::tape_interval` 每次观察全量扫描公开成交带，而成交带
  随成交增长（2200 秒 4964 笔），单次观察成本随累计成交线性上升。实测「事件数/秒恒定
  （635.8 → 637.6）而墙钟/事件涨 42%」。这是性能缺陷，不是市场性质；修复后须重测本表。
  60 逻辑秒短窗口下为 0.037（PASS），所以短测看不见它。

## 3. Stylized facts 五项（SC-502，2200 逻辑秒）

| # | 特征 | 判定 |
|---|---|---|
| 1 | 收益厚尾（超额峰度 z 检验） | **PASS** |
| 2 | 收益无显著线性自相关（lag 1–5，组 B 校正） | FAIL |
| 3 | 波动聚集（lag 1 与 lag 50 都显著，Q-505） | FAIL |
| 4 | 成交量—波动正相关（W = 30） | FAIL |
| 5 | 订单流长记忆（lag 1–100 全正 + 幂律衰减） | FAIL |

达标 1 项，SC-502 要求 ≥ 3 项，**未达标**。E6 不要求达标（见 §4）。

## 4. E6 判定

退出条件 E6 要求的是「冷启动后自发成交、双边盘口可用、六项**逐项如实判定**且未通过项
顶层可见」，**不要求六项全部达标**——价格发现来自策略族异质
（[`ADR-011`](../decisions/011-market-engine-trader-layering.md) §决策 3），而异质性目前
并不成立（见 §5）。按此口径：

- 冷启动后自发成交：✅ 窗口内 135.6 笔/分钟，窗口起点之后仍持续成交；
- 双边盘口可用：✅ 1.000；
- 六项逐项判定且未通过项顶层可见：✅ 报告 `failed` 列出全部 8 项未通过内容；
- 证据标签 `engineering-demonstration`：✅ 随 artifact 落盘。

## 5. 最重要的一条：异质性尚未成立

2200 逻辑秒共 4974 笔成交，taker 构成：

| 族 | taker 笔数 |
|---|---|
| 情绪噪声 | 4964 |
| 均值回归 | 5 |
| 趋势跟随 | 5 |

**价格几乎完全由噪声族一家形成。** 趋势跟随与均值回归族不参与的原因不是死锁，而是
**时间尺度**：两族读 1 分钟 K 线、窗口 3–60 根，2200 逻辑秒只有约 36 根 K 线，其中
`(10, 60)` 这一档全程无法满足，`(5, 30)` 也只在后段勉强可用。

这意味着 §3 的 stylized facts 结果**不是对「异质策略族市场」的检验**，而是对「单一噪声族
市场」的检验。SC-502 的四项未达标在这个前提下不构成对策略层设计的判断。

处置：这是 spec 层面的取舍（改族的时间尺度，或把研究运行的逻辑时长拉到数小时），
不在 T967 内顺手调参数——调参会让「指标改善来自机制修复还是来自调参」无法区分。
承接问题由 `H2-E2`（T973）决定。
