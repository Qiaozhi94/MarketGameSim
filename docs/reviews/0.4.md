# v0.4 检视复盘记录（循环 28—）

> 本文件是 **v0.4 期检视循环的复盘记录**，按版本切分（见
> [`RETROSPECTIVE.md`](RETROSPECTIVE.md) 索引）。每完成一个检视循环
> （`stop_condition_met` 全部满足）追加一条到**当前版本**的文件，不新建文件、
> 不再往单一巨型文件里追加。
> 循环进行中的报告见同目录 `CURRENT-doc.md` / `CURRENT-code.md`（按 `report_type`
> 分文件，同一时间可以有多个并行，均不进 git）；循环内的逐轮细节不保留独立文件，
> 需要时用 `git log --follow -p` 在本文件历史里找回。

---


## 循环 28: 方向变更后全仓文档检视（ADR-010/011 落地后的真源一致性）

- **report_type**: doc-review
- **周期**: 2026-09-20（4 轮：全量扫描 → 修复 → diff-only 复核 → 修复 → 封顶复核 → 检视方下场修复 + 独立核对）
- **基线**: `8b0f518` → `d311d0b`（19 个提交；文档 31 份 + 源码 2 份 + 测试 5 份）
- **触发**: v0.3 签收（2026-09-20）与 ADR-010/ADR-011 方向重构落地后，owner 要求对全仓
  文档做一次一致性检视
- **构成**: 受检面为 `docs/` 全树 89 份 md + `README.md` + `RUN.md`，以及与文档断言直接
  相关的 `tools/spec_validation.py`、`showcase/evidence_index.py`、`experiment/h2/live_market.py`
- **回归测试**: pytest 2733 → 2757 passed（+24：证据盖章 11、成果门交叉校验 5、冻结家族 2、
  复盘门 3、SOP 作用域 1，另有既有断言改造）
- **收尾状态**: **已闭环**——26 条中 23 条 fixed、1 条 tracked（0.4.1/T978）、2 条由检视方
  下场修复；Critical/High 清零；远端 CI run 35500523283（`d311d0b`）5 个 job 全部通过，
  本地 `python tools/verify.py` 全绿

### 完整 issue 表

| ID | 标题 | 严重度 | 分类 | 根因/症状 | 来源 | 状态 | 修复建议 | 修复方案 | 回归测试 | 首次出现轮次 | 修复轮次 | 模式标签 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| d-e4-contradicts-sc503 | 0.4.1 退出条件 E4 与成果门 T973 要求「内生崩盘必须出现」，与 SC-503「两个方向都算达标」直接矛盾 | high | correctness | root-cause | spec-drift | fixed | 删「且出现」，照 AC-506 措辞对齐 | E4 改为「产出存在性判定报告」；T973 写明「出现」不是通过条件及其理由（把它写进收口前提，压力会精确落在调阈值上） | —（纯文本约束；核对为 spec E4 / SC-503 / tasks T973 三处同步） | 1 | 2 | partial-symmetric-fix |
| d-stylized-facts-second-source | 0.4.1 §6 把已冻结的 0.1.2 市场验证协议所拥有的统计特征重新定义为第二真源 | high | correctness | root-cause | spec-drift | fixed | 引用 0.1.2 协议与 metrics/validation.py，仅新增协议未覆盖的两项 | 新增「判定口径拥有者」表，前三项归协议 §3.1/§3.2/§3.3；T971 改为复用既有检验；ADR-011 §决策 3 同步指向协议 | —（实现期由 T961/T971 的 verify 锚点承接） | 1 | 2 | cross-feature-contract-drift |
| d-kurtosis-threshold-vs-ac505 | SC-502 的「收益峰度 >3」与 AC-505「独立正态序列判未通过」在数学上不相容 | high | correctness | root-cause | original-coding | fixed | 改用超额峰度 + 显著性检验口径 | 改为 `k = m4/m2² − 3`、`z = k/sqrt(24/n)` 单侧检验（协议 §3.1），并保留「旧写法为何删除」的说明 | —（由 T971 的正反用例承接） | 1 | 2 | — |
| d-evidence-rebind-restamp | 0.1.5 证据索引自 v0.1 签收后被重绑 29 次（19 次纯重绑），无一次伴随 T215 重跑，等于走了 ADR-005 明确拒绝的方案 B 且不留痕 | high | correctness | root-cause | process-gap | fixed | 三选一并写成 ADR：真重跑 / 兑现 ADR-005 方案 A / attestation 留痕 | owner 裁决选 (c)：新建 ADR-012，索引增 attestation 块（rerun / attested / t215 源码树 / 日期 / 理由），`_validate_attestation` 强制盖章跟着哈希走，R5 在 `rerun: false` 时如实披露「未重跑 T215」与证据真正的产出树；ADR-005 方案 A 维持挂起 | `test_flagship_evidence_index.py::test_rebinding_without_reattesting_is_rejected` 等 11 条（检视方独立变异 8/8 拦截） | 1 | 2 | gate-bypass-by-practice |
| d-prd-h2d2-archived-track | PRD §15 成果门表仍把已归档的「6 训练 + 24 正式采集场景」写为 H2-D2 验收动作 | high | correctness | root-cause | spec-drift | fixed | 删 H2-D2，改指 releases/0.3.md 的范围裁决 | H2-D 行改为「Web 交易终端」并标注归档裁决；同表新增 H2-E / H2-F 两行 | —（文档 diff；ID 侧由成果门交叉校验覆盖） | 1 | 2 | partial-symmetric-fix |
| d-ownership-map-missing-north-star | owner-research-question.md 自称「owner-facing 设计第一判据」，却不在 docs/README.md 所有权地图与 SOP 原则表内 | high | correctness | root-cause | spec-drift | fixed | 所有权地图与 SOP 同时登记 | docs/README.md 所有权表新增一行 + 职责边界条款（冲突以北极星为准，但结论形态仍归 PRD §13.5/§15）；SOP §5 增 owner-facing 复核对照项、§6 增入口 | —（文档 diff；结构侧由 check_ownership_index 覆盖） | 1 | 2 | — |
| d-architecture-l2-name-collision | 架构文档的 L2 = `agent/`，ADR-011 的 L2 = 交易者策略层，同一仓库内「L2」有两个互不相同的含义 | high | correctness | root-cause | spec-drift | fixed | 架构拆 L2a/L2b，或 ADR-011 改用非数字层名 | 架构 §1 拆 L2a（代理运行时，写决策事件）/ L2b（strategy_layer，不写事件）+ 「不要合并回去」的理由 + 依赖规则逐条改写；ADR-011 补对应关系 | —（文档 diff） | 1 | 2 | cross-feature-contract-drift |
| d-prd-stale-after-0.3-signoff | PRD 头部与「当前可见基线」仍停在 v0.3 草案期 | medium | correctness | root-cause | spec-drift | fixed | 改产品阶段、当前规格链接与「下一动作」整句 | 版本升 0.6.1；三行重写；下一动作改为评审批准 v0.4 两个里程碑；ADR 清单补 003/004/005/012 | —（文档 diff；链接由 check_docs_links 覆盖） | 1 | 2 | — |
| d-readme-stale | 顶层 README 仍列 N-of-1 为 0.3.1 交付；ADR 索引缺 5 份；任务号段表只到 T7xx | medium | correctness | root-cause | spec-drift | fixed | 三处同时更新 | 当前规格改 v0.4 并标注 v0.3 已签收与 N-of-1 归档；号段补 T8xx/T9xx；ADR 清单补齐 9 份并加「与 PRD 头部是同一份集合」的同步约束句 | —（文档 diff；交付链接由 test_delivery_entry.py 覆盖） | 1 | 2 | — |
| d-runmd-missing-live-market | live_market 已有可用 CLI，但未写入 RUN.md，主干载体只能靠读源码启动 | medium | correctness | root-cause | spec-drift | tracked | 由 0.4.1 T978（成果门 H2-E3）承接 | 维持 tracked：命令本身是 T978 的交付物，提前写入等于让成果门失去产物；RUN.md 新增「待接入入口」小节登记现状、承接任务与「不得随 Phase 一起消失」（裁决记录 #2） | — | 1 | — | — |
| d-runmd-archived-collection-stage | RUN.md 仍在指导运行已归档的采集态（`--stage training/formal` + preview 证据） | medium | correctness | root-cause | spec-drift | fixed | 加归档标注或移入归档小节 | 移入「已归档入口」小节，写明归档是 ADR-010 的范围裁决、机械保留可复活、「在此之前不要按这些命令采集数据——产出的 artifact 没有任何轨道消费」 | —（文档 diff） | 1 | 2 | partial-symmetric-fix |
| d-0.4.1-prerequisites-wrong | 0.4.1 的 prerequisites 只写 0.3.1，实际上游是 0.3.2 | medium | correctness | root-cause | original-coding | fixed | frontmatter 增加 0.3.2，与 design/related_features 对齐 | frontmatter 补 0.3.2 | `tools/validate_spec_lifecycle.py`（前置存在性与无环） | 1 | 2 | — |
| d-gate-id-not-cross-checked | 成果门 H2-E1/E2/E3 只存在于 PRD 散文未进 §15 表；门禁也不交叉校验 ID | medium | correctness | root-cause | process-gap | fixed | PRD 补行 + 门禁交叉校验 + 变异验证 | PRD §15 成果门表补 H2-E / H2-F 两行；spec_validation 新增「tasks 的成果门 ID 必须出现在 PRD §15」交叉校验，含前缀误匹配防护 | `test_spec_lifecycle.py::test_repository_outcome_gate_ids_are_all_registered_in_prd`（检视方独立变异 H2-E1→H2-E9 变红） | 1 | 2 | gate-not-enforced |
| d-owner-experiment-orphan | `experiment/h2/owner_experiment.py` 与 `.html`（约 300 行）全仓文档零引用，为已归档的训练轨而建 | medium | quality | root-cause | spec-drift | fixed | 写入 RUN.md 或明确标注归档保留 | 0.3.2 spec 范围外小节新增「归档保留的机械清单」（protocol/assignment/session 守卫、owner_experiment.py/.html、对应集成测试），写明「不是遗留垃圾，也不属于本里程碑交付面」；RUN.md 归档小节登记启动命令 | —（文档 diff） | 1 | 2 | orphaned-after-pivot |
| d-north-star-1-unscheduled | 北极星研究问题 #1（人类扰动）无任何里程碑承接，只有 PRD 一行「H2-F 待立项」 | medium | correctness | root-cause | spec-drift | fixed | 移出 0.4.1 Phase 3 腾出工期并立 H2-F 三件套 | owner 裁决只立载体、不移出 Phase 3（裁决记录 #1）：新建 0.4.2-human-perturbation 三件套（15 任务 / 2 Phase，prerequisites 0.4.1，experiment-preview），版本根 spec/README/traceability 与北极星 §5、PRD §15 同步登记 | `tools/validate_spec_lifecycle.py`（新里程碑 gate v1 全量校验） | 1 | 2 | — |
| d-h2f-falsifiability-bridge | 北极星「扰动即数据、不存在错误用法」与 PRD §13.5 可证伪要求之间缺桥接条款 | medium | correctness | root-cause | spec-drift | fixed | 写明同种子对照的效应量命题形式并冻结 artifact 格式 | PRD §15 新增「人类扰动问题的可证伪化桥接条款」（本节唯一拥有）：同种子对照效应量命题、三指标分布移动+区间+失效边界、禁止「人类能否造成崩盘」式自我实现命题、退化臂输出退化判定而非零效应、artifact 须在第一场会话前冻结；北极星加工程侧注记指回 | —（0.4.2 SC-603/AC-605 承接实现侧断言） | 1 | 2 | — |
| d-h2e1-gate-before-dependency | 成果门 H2-E1（T967）引用「市场质量六项达标」，但四个异质策略族要到 Phase 2 才实现 | medium | correctness | root-cause | original-coding | fixed | T967 只要求逐项判定，达标移至 H2-E2/E3 | 新增退出条件 E6 作为 Phase 1 判定口径；SC-501 加「全部达标的判定点是 H2-E2 及之后」并禁止据此判 Phase 1 失败或调低门限；T967 改引 E6；traceability 挂载 E6 | `tools/validate_spec_lifecycle.py`（AC / 退出条件引用存在性） | 1 | 2 | gate-before-dependency |
| d-nfr501-risk-deferred | NFR-501 的 8–10 倍性能缺口是唯一可能拖垮版本的风险，退路却留到 Phase 2 中段再拍板 | medium | quality | root-cause | original-coding | fixed | 预先写入退路顺序与各自代价，不留「实测后拍板」 | §7 决策表新增「性能退路与其代价」行：①做市商重报价/撤单策略（无 stylized facts 代价）→ ②拉长观察间隔（样本量需更长逻辑时长）→ ③降代理数（厚尾与波动聚集变弱），并禁止取用 ③ 时同时下调 SC-502 门限；design §9 同步 | —（实测由 T970 的 tests/performance 承接） | 1 | 2 | — |
| d-architecture-stale-modules | 架构文档头部停在「当前 v0.2 交互层」，分层表缺 `src/` 下 9 个包 | medium | quality | root-cause | spec-drift | fixed | 补共享设计引用与各包归属层 | 共享设计引用补 v0.3/v0.4；分层表补 showcase/interactive（L4）、bench/metrics/robustness/evidence（L3）、hook（L1），新增 L0 跨层基础层（config/rng/schema）与对应依赖规则 | —（文档 diff） | 1 | 2 | — |
| d-adr011-stale-followups | ADR-011 末尾两条「后续行动」已过时 | low | quality | root-cause | spec-drift | fixed | 追加注记而非改写历史决策正文 | 改为「2026-09-20 复核更新」，两条过时项用删除线保留原文并接裁决结果（N-of-1 已归档见 releases/0.3.md；CLI 已存在，待补的只是 RUN.md 注册），另补 0.4.2 承接与 L2b 对应关系 | —（文档 diff） | 1 | 2 | — |
| d-0.4.1-header-stale | 0.4.1 spec 标题行写 `Target: v0.3`，PRD 来源写 §15 H2-D | low | quality | root-cause | original-coding | fixed | 改为 v0.4 / §15 H2-E | 已改，并标注三个成果门 ID | —（文档 diff） | 1 | 2 | — |
| d-retrospective-file-size | RETROSPECTIVE.md 已 1264 行，单文件追加成本只升不降 | low | quality | root-cause | process-gap | fixed | 按版本切分 + 索引 | 切为 0.1.md（循环 0—19）/ 0.2.md（循环 20）/ 0.3.md（循环 21—27），RETROSPECTIVE.md 降为索引并写明回写规则；既有跨文件断言改 glob | `test_review_process.py::test_retrospective_index_holds_no_cycle_bodies`（检视方核对循环标题 25→25、表格行 305→305，零丢失） | 1 | 2 | — |
| d-sop2-vs-0.4.2-statistics | SOP §2「interactive 运行不进任何统计」与新建 0.4.2 的效应量报告（SC-603）在文本上直接冲突，SOP 侧无任何指针 | medium | correctness | root-cause | fix-regression | fixed | SOP §2 补作用域说明指向 0.4.2 Q-601，不改 0.4.2（它已显式挂起该裁决） | SOP §2 补作用域说明：争点（分析单元是 seed 还是人）归 Q-601，**裁决前本条按字面全额生效**，裁决落地须同步修订本条；配随裁决状态翻面的门禁 | `test_spec_lifecycle.py::test_sop_interactive_statistics_clause_tracks_the_open_human_perturbation_adjudication`（检视方独立变异：删指针→红；勾上 Q-601 不动 SOP→红） | 2 | 3 | cross-feature-contract-drift |
| d-sf-family-membership-undeclared | 0.4.1 SC-502 沿用 0.1.2 协议的「组 A 家族校正」但成员集合不同，未声明两处判定不可直接比较 | low | correctness | root-cause | fix-regression | fixed | 补成员差异说明 + 禁止并入既有家族实例；lag 1/lag 50 组合判据不当场拍板 | SC-502 补成员差异声明与「不得并入 `build_market_validation_matrix` 既有家族实例」；AC-505/T961 加守卫要求；lag 组合判据立为 Q-505 交 T961 冻结 | `test_validation.py::test_kpi005_family_a_membership_is_frozen`、`::test_adding_0_4_1_features_to_the_frozen_family_would_change_kpi005_verdicts`（检视方手算复核 Holm 阈值 0.05/4=0.0125 vs 0.05/6=0.00833） | 2 | 3 | — |
| d-ci-evidence-gate-title-filter | 复盘 CI 证据门的文件选择已版本无关，条目选择仍写死「标题含 H2 或 0.3」——22 条里只覆盖 7 条，v0.4 起的条目自动逃出 | medium | correctness | root-cause | fix-regression | fixed | 覆盖面按版本文件划定 + 冻结豁免集合 + 去掉名字里的 h2/H2 + 变异验证 | 条目过滤去掉标题关键字；豁免集合冻结为 v0.1/v0.2 并由新测试钉死；文件枚举从 glob 改为「除白名单外全部纳入」（变异 C 实测 `0.2.5.md` 会整条绕过）；版本分界改数值元组比较（字典序下 `0.10.md < 0.3.md`）；函数与测试改名 | `test_review_process.py::test_version_retrospectives_record_only_ci_evidenced_closure`、`::test_only_pre_practice_versions_are_exempt_from_the_ci_evidence_gate`（5 条变异：A/B/C/D 红、E 绿） | 3 | 4 | partial-symmetric-fix |
| d-q505-numbering-out-of-order | 0.4.1 spec §8 的问题编号顺序为 Q-501/502/503/505/504 | low | quality | root-cause | fix-regression | fixed | 把 Q-505 移到 Q-504 之后 | 已归位 | —（文档 diff） | 3 | 4 | — |

### 裁决记录

```
#1 · d-north-star-1-unscheduled · partial→fixed · 建议含两部分：(a) 立 H2-F 载体；
     (b) 移出 0.4.1 Phase 3 腾出工期。owner 2026-09-20 裁决采纳 (a)、保留 Phase 3。
     (b) 属排期建议而非缺陷，缺陷本体（研究问题 #1 无承接载体）已由 0.4.2 三件套关闭。
     证据：docs/features/0.4/0.4.2-human-perturbation/spec.md（gate v1 通过）。· 裁决轮次 2
#2 · d-runmd-missing-live-market · tracked 维持 · live_market 命令是 0.4.1 T978 成果门的
     交付物，提前写入等于让成果门失去产物。检视方核对同意：RUN.md「待接入入口」小节已
     记录现状、承接任务与「不得随 Phase 一起消失」。证据：RUN.md 末节、tasks.md T978。
     · 裁决轮次 2
#3 · FIX-log Round 2「未实施项」· 接受 · 修复方声明未把「新建里程碑就绪检查加阻断规则
     冲突自查」写进 features/README，理由是当轮范围为 diff-only、规则化属下一循环。
     检视方同意：该建议来自模式观察而非 issue 表条目，其落点是本节的模式教训 + 单独立
     任务，不构成本循环的未修复部分。· 裁决轮次 3
#4 · 第 4 轮角色合并 · 检视方下场修复 · 第 4 轮核对发现「问题都已修复」的声明与仓库状态
     不符：HEAD 未变（`ff1f6e9`）、工作树干净、FIX-log 无 Round 3 声明、两条 finding 的
     目标行原样存在。按协议第 7 节升级路径 (b)（角色合并）由检视方亲自修复，修完显式切回
     检视视角独立核对（覆盖/豁免集合实测、悬空引用扫描、变异矩阵），未自批。· 裁决轮次 4
```

### 模式教训

- **`origin` 分布**：spec-drift 13、original-coding 5、fix-regression 4、process-gap 3。
  压倒性是契约漂移——问题不在方向变更本身的质量，而在**改动的传播半径没有被机器覆盖**：
  ADR、里程碑、release 都改了，PRD 头部/成果门表、README、RUN.md、架构文档、所有权地图
  五处派生文档全部没跟上。
- **修复引入率 4/26 ≈ 15%**，低于协议引用的 20–30% 经验值，且四条**全部出在新增面**
  （0.4.2 三件套、被扩写的 SC-502、被改成 glob 的测试、Q 编号），19 条定点改写零回归。
  结论：本仓库的定点修改是安全的，**新增面才需要第二轮**。
- **`partial-symmetric-fix` 累计 4 次**，是本循环第一大模式：E4/T973、PRD 两张表、
  RUN.md 两形态、复盘门的「文件选择 vs 条目选择」。共同形状是**一个概念有成对的两处落点，
  改了语义明显的那处、漏掉机械的那处**。流程对策：归档 / 改名 / 放宽类改动，提交前用
  `grep -rn` 列出该概念的全部出现位置并贴进提交信息——最后那条如果做了，
  `grep H2` 会让 `_h2_cycle_entries` 立刻现形。
- **`cross-feature-contract-drift` 累计 3 次**：新文档自身自洽，但与另一份文档的既有条款
  在同一命题上给出不同判断（ADR-011 vs 架构 L2、0.4.1 vs 0.1.2 协议、0.4.2 vs SOP §2）。
  **后续行动（未实施，需单独立项）**：在 `validate_spec_lifecycle` 里为新建里程碑加一项
  「阻断规则冲突自查」的结构化声明位——只写进散文的规则，按下一条的规律活不过两个版本。
- **门禁类失效的三种形态，本循环各中一次**，这是最值得带走的一条：
  ① 规则写在文档里、机器不执行（成果门 ID 一致性）；
  ② 机器执行了、实践绕过且不留痕（证据索引重绑 19 次）；
  ③ 机器执行了、**覆盖面写死**（复盘 CI 证据门只管 7/22 条）。
  ③ 比 ① 危险，因为它**看起来是绿的**。新增或修改门禁时必须同时回答「谁不在它的
  管辖内」，并为「将来新增的同类对象」留默认纳入路径——本轮的修法（除白名单外全部
  纳入 + 冻结豁免集合 + 数值版本比较）可作为模板。
- **变异验证救了自己一次**：检视方修复 `d-ci-evidence-gate-title-filter` 的第一版
  实现仍用 glob `[0-9].[0-9].md`，变异 C（新增 `0.2.5.md`）直接放行——如果只跑
  「当前仓库能通过」就提交，这条门会带着一个新洞上线。**门禁改动必须当场做反向变异，
  不是跑一遍绿就算数。**
- **「已修复」的口头声明在本循环出现过一次为假**（第 4 轮：HEAD 与上一轮字节级相同）。
  协议要求的「独立核对而非采信」不是形式主义——本仓库历史上已有同类记录。
- **裁决分布与建议命中率**：26 条中 23 条实质采纳、1 条部分采纳（#1）、1 条维持 tracked
  （#2）、0 条 rejected，命中率 23/25。无全盘接纳（检视凑数）或全盘拒绝（检视空转）的
  极端。**四处修复超出建议**：ADR-012 的 `t215_source_tree_sha256` 字段、性能退路表的
  「不得同时下调门限」约束、SOP 门禁的「随裁决翻面」设计、组 A 家族的「并入会改判」
  演示用例——修复方不是照单执行。
- **存活轮数**：21 条 first_seen=1 的在第 2 轮关闭，2 条 first_seen=2 的在第 3 轮关闭，
  2 条 first_seen=3 的在第 4 轮关闭。没有跨多轮拖延的条目，每轮 diff 严格收窄
  （31 文件 → 5 文件 → 2 文件），符合「收敛在加快」而非「越修越多」。

---


## 循环 29: 0.4.1 需求设计专项文档检视（owner 裁决落地 + 就绪门）

- **report_type**: doc-review
- **周期**: 2026-09-21 — 2026-09-22（3 轮：全量扫描 → 修复 → diff-only 复核 → 封顶复核；
  最后一条 High 等 owner 裁决 DQ 后关闭）
- **基线**: `2c0c189` → `bacda30`（4 个提交：b274c53、5262971、a0b1753、bacda30）
- **触发**: owner 裁决 G1 与 Q-501—Q-505 后，0.4.1 首次申请流转 `ready-for-development`；
  循环 28 是全仓一致性检视，0.4.1 此前没有专项检视与就绪门记录
- **构成**: 受检面为 0.4.1 `spec.md` / `design.md` / `tasks.md`，以及与本次决议相关的
  ADR-014、架构文档 §1、版本 README
- **回归测试**: 文档 finding，闭环证据为 `tools/validate_spec_lifecycle.py`；就绪门做了变异
  验证——重开 DQ-503 时校验报「DQ-503 仍未关闭」变红，恢复后变绿。pytest 2773 passed
- **收尾状态**: **已闭环**——11 条全部 fixed，Critical/High 清零；0.4.1 流转
  `ready-for-development`；远端 CI run 35622673383（`bacda30`）5 个 job 全部通过，本地
  `python tools/verify.py` 全绿

### 完整 issue 表

| ID | 标题 | 严重度 | 分类 | 根因/症状 | 来源 | 状态 | 修复建议 | 修复方案 | 回归测试 | 首次出现轮次 | 修复轮次 | 模式标签 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| d-anchor-random-vs-boundary | Q-501 决议「首单方向取自随机流」与 spec §3「不得靠随机数硬凑第一笔」矛盾 | high | correctness | 根因 | fix-regression | fixed | 改为按装配顺序奇偶交替 | 第 1 轮改为全局奇偶交替（5262971）；owner 复议后改为**族内**交替 + 奇数族余数轮流分配，位置从 0 计（bacda30） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | partial-symmetric-fix |
| d-design-questions-open | design §10 DQ-501/502/503 未裁决，就绪门要求全部关闭 | high | correctness | 根因 | original-coding | fixed | owner 裁决后写回 | owner 按检视方建议裁决：锚放 goal.py 共享分支且来源可插拔；四层级够用、外部信号留 I3；外部源声明 `blocking` 缺省 true（bacda30） | `validate_spec_lifecycle`（变异：重开 DQ-503 即红） | 1 | 3 | — |
| d-info-tier-name-collides-with-arch-layers | 分级信息集 L0—L3 与架构分层 L0—L3 撞名 | medium | correctness | 根因 | original-coding | fixed | 改名 I0—I3 | design §4、T964、DQ-502 改为 I0—I3，并注明撞名原因（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | cross-doc-name-collision |
| d-design-ac506-says-fail | design §8 AC-506「无事件时如实未达标」与 SC-503「两个方向都算达标」矛盾 | medium | correctness | 根因 | spec-drift | fixed | 改为「不存在」结论同样达标 | AC-506 行改为存在性判定、两个方向都算达标（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | partial-symmetric-fix |
| d-stat-window-never-opens | 统计窗口从最后一个代理退出冷启动后开始，未定义永不开启时的处置 | medium | correctness | 根因 | fix-regression | fixed | 判「不适用」且顶层可见 | Q-501 补：运行结束仍有代理未退出时报告判「不适用」且顶层可见，禁止截断窗口或剔除代理（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | — |
| d-goal5-alphamill-adapter | spec §1 目标 5 仍含 alphamill 适配器，与 §3 范围外矛盾 | medium | correctness | 根因 | spec-drift | fixed | 改为外部信号注入接口 | 目标 5 改写；design §2 `external.py` 注释同步（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | partial-symmetric-fix |
| d-anchor-empty-book-price | Q-501「按对手方最优价下单」未定义对手方为空时的行为，而冷启动正是空簿场景 | medium | correctness | 根因 | original-coding | fixed | 空簿时以初始价挂限价 | Q-501 与 T963 补：对手方为空时以运行头初始价挂限价，奇偶交替保证两侧相遇（a0b1753） | `validate_spec_lifecycle`（文档门禁） | 2 | 2 | — |
| d-ewma-semantics-no-task | spec §3 范围内「EWMA 语义澄清」无任务承接 | low | correctness | 根因 | original-coding | fixed | 并入 T963 | 并入 T963 交付描述（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | — |
| d-design-l1-diagram-stale | design §2 的 L1 图未跟上 ADR-013 与 L2b 称谓 | low | quality | 根因 | spec-drift | fixed | 标注 L1a/L1b、L2b | 图中标注 L1a/L1b（含 hook/）与 L2b（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | — |
| d-design-main-entry-stale | design §6「恢复 `__main__`」已过时，CLI 已存在 | low | quality | 根因 | spec-drift | fixed | 改为 RUN.md 登记由 T978 承接 | 改为 CLI 已存在、RUN.md 登记由 T978 承接（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | — |
| d-updated-date-design | design frontmatter `updated` 未随改动更新 | low | quality | 根因 | fix-regression | fixed | 改为 2026-09-21 | 已改（5262971） | `validate_spec_lifecycle`（文档门禁） | 1 | 1 | — |

### 裁决记录

```
#1 · d-anchor-random-vs-boundary · fixed（方案经 owner 复议） · 第 1 轮修复为全局奇偶交替；
     owner 追问「随机数还是奇偶」后，检视方指出全局交替会让单代理/奇数代理的族整族偏向
     一侧，owner 2026-09-21 采纳族内交替 + 奇数族余数轮流。随机方向被否的证据：不保证两侧
     都有首单（空簿下可能重新死锁），也不保证中性开局。· 裁决轮次 3
#2 · d-design-questions-open · fixed（owner 裁决） · 三项均按检视方建议裁决，证据见
     design §10 DQ-501—DQ-503 的决议文字。· 裁决轮次 3
```

### 模式教训

- **`origin` 分布**：original-coding 4、spec-drift 4、fix-regression 3。3 条修复引入**全部**
  出在「把 owner 决议写回文档」这一步——新写的决议文字与既有条款（§3 边界场景）、与自身
  未覆盖的边界（窗口不开启、空簿定价）冲突。再次印证循环 28 的结论：**新增面才需要第二轮**，
  定点改写零回归。
- **第 2 轮不是走过场**：`d-anchor-empty-book-price` 是首轮漏检，只在第 2 轮复核 diff
  相邻文字时出现。若首轮后直接宣布就绪，T963 会带着一个在冷启动（恰好是空簿）下无定义
  的规则开工。
- **`partial-symmetric-fix` 再现 3 次**（累计 7 次）：AC-506 在 spec/tasks 里由循环 28 修过、
  design 漏了；alphamill 适配器在 §3 移出、§1 目标与 design §2 没跟。**同一概念成对落点只改
  一处**仍是本仓库第一大模式。
- **`cross-doc-name-collision` 首次出现**：信息层级 L0—L3 与架构分层 L0—L3 撞名，是架构
  文档 §1「两个 L2」教训的原样重演——那条教训写在散文里，没有机器守卫，所以新文档又踩了。
- **项目约定与通用检视协议的差异**：通用协议要求 `CURRENT-*-<FID>.md` / `FIX-log-<FID>.md`
  入库；本仓库 `.gitignore` 规定过程稿不入库、修复日志名为 `FIX-log.md`。按带后缀的名字
  建文件会让 `test_review_process.py` 把它当成版本复盘文件而变红。以项目约定为准。
- **裁决分布与建议命中率**：11 条全部采纳，0 rejected / 0 partial；建议命中 10/11，唯一偏离
  是 #1——owner 复议让方案比建议更好（族内交替优于全局交替）。
- **存活轮数**：9 条 first_seen=1 在第 1 轮关闭；1 条 first_seen=2 在第 2 轮关闭；
  `d-design-questions-open` 存活 3 轮，原因是等 owner 裁决，不是修复失败。
