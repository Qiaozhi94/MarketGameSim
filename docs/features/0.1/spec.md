---
kind: version-spec
id: v0.1-belief-testing-laboratory
version: "0.1"
status: in-progress
created: 2026-07-31
updated: 2026-08-12
---

# Feature Specification: Belief Testing Laboratory

**规格编号**：v0.1-belief-testing-laboratory  
**状态**：见 frontmatter（唯一状态真源）。当前里程碑 0.1.3 在研，0.1.1—0.1.2 已
达成退出。根规格转 Stable 的条件是 **0.1.4** 退出（完整 v0.1 签收）**——该条件待
[ADR-003](../../decisions/003-goal-driven-agents-and-flagship-identification.md)
定案；若 Accepted，条件调整为新增里程碑 0.1.5 退出，见本文「未闭合事项」**。  
**创建日期**：2026-07-31　**更新日期**：2026-08-02  
**关联 PRD**：[`../../market-game-sim-prd.md`](../../market-game-sim-prd.md) v0.4.0  
**架构**：[`design.md`](design.md)　**里程碑**：[`0.1.1`](0.1.1-minimal-kernel/spec.md) · [`0.1.2`](0.1.2-leverage-and-first-experiment/spec.md) · [`0.1.3`](0.1.3-robustness/spec.md) · [`0.1.4`](0.1.4-replay-and-report/spec.md)  
**已生效决策**：[ADR-001](../../decisions/001-numeric-and-serialization-contract.md)
（数值与序列化口径）、
[ADR-002](../../decisions/002-same-timestamp-event-scheduling.md)（事件调度与因果链）；
其余设计决策见本文 §「设计决策与理由」  
**实现合同**：[撮合](../../contracts/matching.md)、
[事件 Schema](../../contracts/event-schema.md)、
[退化状态](../../contracts/degenerate-states.md)、
[代理策略](../../contracts/agent-strategy.md)、
[账户与保证金](../../contracts/margin-and-account.md)、
[验收向量](../../contracts/acceptance-vectors.md)、
[指标字典](../../research/metrics-dictionary.md)

## 问题与目标

建立一个本地运行、订单簿驱动、**含杠杆与强制平仓**的可复现市场实验环境，使交易信念
能够被改写成条件性命题并接受可证伪的检验。

**第一版的全部范围由一个旗舰问题决定**（PRD §3.1）：

> 在无外生价值冲击的加密式永续市场中，**杠杆上限分布**与维持保证金率是否足以产生
> 自我强化的价格崩盘？

与该问题无关的能力一律后移。

模型中不存在外生基本价值过程；价格路径完全由订单流互动内生产生（D-4）。

## 非目标

- 不预测真实证券或加密资产的未来价格，不生成交易信号。
- 不把仿真结论外推至真实市场（PRD §13.2）。
- 不连接交易所、券商、钱包或真实资金。
- 不预设「庄家」角色——不对称收益必须作为结果涌现（PRD §13.1）。
- 第一版不实现：**股票式制度**（收盘、隔夜、T+2、熔断、融券）、大资金执行者、
  订单流预知者、经济含义的 PnL 归因、组合保证金、多品种、期权、策略在线学习、
  人在环交互。

## 用户场景与验收

### US-1：运行可信市场（P1）

作为研究者，我可以用固定配置与种子运行含杠杆的市场仿真，获得逐事件一致的结果，
并确认其统计特征达到 PRD §12 的门槛。

### US-2：检验一条交易信念（P1）

作为交易者，我可以把一条信念（例如「动量因子在此市场有效」）写成对照实验，运行多
种子后得到条件性结论、效应量与失效边界。

### US-3：分解收益来源（P1）

作为研究者，我可以把任一代理的权益变动完整分解为五项会计桥接（价差、即时冲击、
仓位重估、资金费与违约、手续费），从而回答「钱是怎么亏的」而非仅「亏了多少」。

### US-4：观察杠杆连锁（P1）

作为研究者，我可以在不同杠杆上限分布与维持保证金率下，观察下跌是否自我强化，并测量
强平连锁的规模与深度。

### US-5：追溯任一成交（P2）

作为研究者，我可以从任一成交追溯到代理的观察、信念权重、决策与账户变化，且该追溯
由日志因果外键机器验证。

### US-6：制度对照（P2）

作为研究者，我可以用同一参与者配置与种子，在股票式与加密式制度下各跑一次，比较
制度差异带来的结果差异。

## 功能需求

### 市场与撮合

- **FR-001**：实现价格时间优先的单品种限价订单簿，支持限价单、市价单、撤单与部分
  成交。撮合规则见[撮合合同](../../contracts/matching.md)：成交价取 maker 挂单
  价，跨档逐笔拆分为多个 `TRADE_SETTLE`，限价单剩余挂入簿、市价单剩余按 IOC 撤销。
- **FR-002**：市场制度通过约束钩子注入（D-1），钩子只能拒绝或延迟订单，
  **不得改写**。第一版只提供 `crypto_perp_free` 一套配置；接口须能在不改撮合核心的
  前提下接入其他制度。
- **FR-003**：支持 tick size、最小数量与 maker / taker 分离计费（含负 maker 费率）。
  价格、数量与金额一律以最小单位整数承载，手续费为唯一舍入点（ADR-001）。
- **FR-004**：账户模型为**线性永续合约**（[账户与保证金合同](../../contracts/margin-and-account.md)）：
  维护钱包余额、合约仓位、开仓成本、保证金占用、已实现/未实现 PnL。
  **无现金/库存交割，无债务字段**。每一次账户变动以分录形式记录在引发它的事件上
  （事件 Schema §4.2.1）。

### 杠杆与风险

- **FR-005**：支持全仓保证金与双向持仓（做空与做多对称）。`maint_bp`、`target_bp`、
  `leverage_tier` 分布均为可配置项。初始仓位恒为 0，实际杠杆由代理内生建仓产生
  （账户合同 §7）。
- **FR-006**：保证金率低于 `maint_bp` 时标记待强平，按 `grace_ns` 宽限（**v0.1 恒为 0**）后以市价单
  平仓至 `target_bp`（数量算法见账户合同 §4.2）。强平单由 `MARGIN_CALL` 产生，须跨越 `liquidation_latency_ns ≥ 1`
  才能入簿——它是 class 1 → 0 的回退跳转（事件 Schema §1.2）。该延迟是风控反应速度，
  是一个有研究意义的参数：越慢则连锁滑落越深。
- **FR-007**：**强平连锁不得被人为限速或打断**。穿仓按两步分录核销：钱包归零，
  损失以**负数**记入 `exchange_risk_pnl_units`，账户转入 `LIQUIDATED`
  （账户合同 §5）。不采用社会化分摊。
- **FR-008**：强平订单在事件日志中以 `origin = LIQUIDATION` 标识，并携带触发时的
  保证金率与判定依据。

### 参与者

- **FR-009**：代理信念以因子权重向量表达，权重从可配置分布中抽取，**一次运行内固定
  不变**（D-3、PRD §13.3）。从信息集到订单意图的完整管线由
  [代理策略合同](../../contracts/agent-strategy.md)定义，**同一信息集与 RNG
  状态必然产生同一组意图**。
- **FR-010**：因子库至少包含动量、回归、从众、盘口、随机五类，且**必须覆盖不同信息
  源**；每次研究运行须输出因子相关矩阵（D-3）。
- **FR-011**：回归因子锚定内生 EWMA，各代理半衰期从分布中抽取，不得共用同一值。
- **FR-012**：提供库存型做市商。其优势只能由资金、信息、速度、执行能力四个维度的
  显式配置表达，不得写入特殊交易规则。大资金执行者与订单流预知者后移至 v0.2+。
- **FR-013**：代理能力必须显式配置资金、杠杆上限、信息可见范围与提前量、观察频率与
  通信延迟。通信延迟不得为 0（KR-006）。

### 实验与记录

- **FR-014**：实验配置可序列化、含随机种子，支持多种子批量运行、配对对照与参数扫描。
- **FR-015**：按事件 Schema 记录订单、成交、账户分录、强平判定、代理观察与决策，
  含因果外键；账户快照频率可配置。
- **FR-016**：输出市场层指标（收益、波动、点差、深度、成交量、撤单率、价格冲击）与
  代理层指标（权益、仓位、**杠杆率**、交易成本、PnL），均按时间序列输出。
- **FR-017**：把每个代理的权益变动做**会计桥接**——价差、即时冲击、仓位重估、
  资金费与违约、手续费五项（指标字典 §5.2），**残差恒为 0**。该分解是代数恒等式而非归因约定，
  互斥性可证明；手工验算示例见 §5.2.3。
- **FR-018**：为退化状态定义确定行为——破产、穿仓、连环强平、强平失败、单边簿、
  长时间无成交、价格触界。领域层不得产出未定义状态；输出层缺失值写 `null`
  （ADR-001 §6）。

### 呈现

- **FR-019**：提供单文件 HTML 回放器，以事件日志为唯一输入逐帧呈现价格、订单簿、
  代理状态与**强平事件**，支持拖拽、变速与暂停。回放与报告同源，仿真内核不依赖
  呈现层。
- **FR-020**：提供 K 线视图，周期定义见指标字典 §1.9。

## 设计决策与理由

本章记录设计选择及其被排除的备选方案。这些决策**尚未被任何代码检验**——若实现中
发现某条不可行或代价过高，应在此处修订并说明，而不是悄悄偏离。被实践反复确认、
且跨规格生效的决策，将来再提炼为 ADR。

### D-1：市场制度以约束钩子表达（FR-002）

撮合核心不含任何制度判断，制度通过钩子在固定时点注入：订单准入、时段状态、结算
规则、保证金判定、价格边界。**钩子只能拒绝或延迟，不能改写订单**——静默修正会使
代理的实际行为偏离其声明策略，让行为归因失效。

同一份参与者配置能在股票式与加密式下跑对照，制度因此成为**可研究变量**，而不只是
背景设定。这也是不做单一制度的主要理由：加密式没有收盘，能把「隔夜」这个混淆因素
完全消除；股票式则能表达融资盘的隔夜爆仓。两者互为对照。

代价：第一版工作量显著上升（收盘、T+2、熔断都要实现），且 0.1.2 的基准验证要做两遍。
钩子调用点必须在实现前定死——遗漏一个时点（例如「收盘时强平判定」）会导致整类制度
无法表达。

### D-2：杠杆与强制平仓进第一版（FR-005—FR-008）

不推迟的理由：保证金逻辑渗透进订单准入的每一处，不是可以后期叠加的模块。无杠杆
基准跑通后再加，等于把账户、准入、守恒断言与退化状态重做一遍。

**强平不限速、不打断连锁。** 限速本身就是研究者注入的隐藏变量，且恰好抑制了要观察
的现象——一次强平推动价格，可能使下一个账户跌破维持线，这个正反馈正是研究对象。

**穿仓损失记入交易所风险账户，不做社会化分摊。** 分摊是加密交易所的真实做法，但它
会在代理之间引入一条与交易行为无关的财富转移通道，污染 FR-017 的收益来源分解。若
将来要研究分摊机制，应作为独立配置项引入。

第一版只做单一标的、全仓保证金；不做组合保证金与风险度分层——它们的复杂度与研究
价值不成比例。

### D-3：连续信念空间取代离散代理类型（FR-009—FR-011）

**共同动机 + 异质信念**，而非共同目标 + 在线搜索。真实市场里几乎所有参与者都想
赚钱（动机相同），但对「什么会让价格上涨」的理解各不相同（信念相异）。价值投资者、
K 线交易者、情绪交易者的差别不在目标函数，而在这个信念上。

「想赚钱」本身不产生任何行为——它无法告诉代理此刻该买还是该卖。从动机到订单，中间
必须有一个对价格如何变动的信念。因此模型给代理的是信念，不是优化能力。

**排除在线优化（RL / 遗传算法）的理由**：它看似消除了「研究者钦定规则」，实则把
自由度从看得见的交易规则转移到看不见的学习机制——探索率、记忆长度、适应频率、策略
表示空间同样由研究者选定，同样能决定结论，而且更难被审查。评审者一眼能看穿「上涨
则买入」是循环论证，却很难看穿「变异率 0.05、每 200 步适应一次」如何决定了厚尾
结论。

一个支持该选择的经验事实：真实市场中持续亏损的群体长期存在，且亏损者通常不切换到
正确策略，只是换参数继续。若代理能有效优化，这个群体不该存在。**「人人追求利润
最大化」在动机层为真，在行为层为假。**

**因子库须覆盖不同信息源。** MACD、均线交叉与「动量」本质同族（都是价格历史的线性
滤波），堆砌它们只会制造表面的多样性——因子高度共线，同涨同跌，反而更易共振。扩充
因子库的判据是「是否引入新的信息源」，不是「是否是一个新指标」。

局限：因子库的构成仍是研究者的先验，**只能减弱不能消除**，必须显式声明并做敏感性
检验。

### D-4：不引入外生基本价值（A-001）

模型中不存在基本面信息通道，价格完全由订单流互动内生产生。

**这是一项实验隔离选择，不是对技术分析公理的认可。** 删除其他信息源只表示模型选择
隔离订单流与价格反馈机制，以便研究它们**是否足以**产生目标现象；它不表示价格已经
包含真实世界的全部有用信息。把「无外生基本价值」等同于「价格包含一切」是过强的
推论——前者是建模范围的声明，后者是关于真实市场的论断。

隔离带来的好处是：「技术因子是否有效」从一个前提变成了可以在该模型族内被证伪的
问题——在一个纯粹由订单流形成价格的市场里，技术因子能否获得超额收益？在什么参与者
结构下能？两个方向的答案都有价值，但结论的适用范围只到模型族边缘为止。

代价是内生锚的**自指结构**（价格锚定于自身历史），其稳定性不能假定，有三种失效
形态：锁定（锚跟随过快，回复力名存实亡）、发散（制动失效）、周期振荡（锚过于僵化，
产生由半衰期决定的人造周期）。第三种尤须警惕——它外观酷似真实市场周期，判据是收益
自相关函数在锚半衰期对应的 lag 附近出现显著峰（A-004）。

### D-5：离散事件内核而非固定步长（KR-001）

固定步长会把「速度」压缩成步内顺序，使延迟差异失去分辨率——而速度正是 A-003 的
四个能力维度之一。离散事件让延迟成为连续可配置参数，强平连锁对事件顺序的精确性
要求也高于固定步长能提供的粒度。

代价是所有时间语义都要显式建模（KR-002—KR-006），不能依赖「每步都发生」的隐式约定。

### D-6：自建最小内核，不采用现成 ABM 框架（KR-005）

ABIDES、PAMS 等框架不含保证金与强平模型，要用就得改它们的账户层；而 ADR-001/006
的约束——最小单位整数、逐事件整数精确守恒、因果外键、规范序列化到字节唯一——外部
框架一样都不提供。在别人的抽象上强加这些约束，比自己写更费劲。

该判断在方向重置（含杠杆、制度钩子、因子库）后**重新确认过**：工作量确实上升了，
但需要的正是框架不提供的那部分。若将来要复用强化学习训练环境，须重新评估。

### D-7：可视化以事件日志回放为主（FR-019）

回放器只消费事件日志、不被核心层引用，因而不占用关键路径，其失败不影响内核正确性；
回放与报告同源，保证「看到的」与「统计的」是同一份数据。

**远期张力已知**：第三层的可玩模拟器需要实时渲染与逻辑时间-墙钟挂钩，这与离散事件
内核「尽可能快地跳过空闲时间」的本性冲突。

为免第一版做出堵死该路径的设计，现在就定下三条约束（Q-016），实现细节留到 v0.2+：

1. **确定性的定义扩展为「相同代码 + 配置 + 种子 + 输入序列」。** 人类输入记录为
   事件流（`AGENT_DECIDE` 携带 `rule_id = "human"`），回放时作为外生输入重放，
   NFR-001 的逐帧一致照常成立；
2. **两类运行在日志头部标记 `run_mode`**：`research`（无人类输入，进统计与结论）
   与 `interactive`（有人类输入，仅用于直觉建立与教学，**不进任何统计**）。样本量
   1、不可复现、有学习效应，KPI-010 直接排除它；
3. **实时调速是内核的运行模式，不是新内核。** 事件推进节流到墙钟，逻辑时间语义
   不变——第一版无需实现，但内核不得做出「事件必须尽快处理」的隐含假设。

人类在**速度维度上必然劣于所有代理**（反应时间几百毫秒 vs 配置的 5—50 毫秒），因此
交互模式下可扮演的是**资金规模与执行策略**，不是速度或信息优势。这一限制须写入
第三层的需求，避免把交互体验误当作能力对照实验。

## 参数取值

本章的取值**已定死，可直接实现**。它们不是待确认项；标注「须实测校准」者指首次
运行后回头检验，若与预期不符按 PRD §10 变更规则修订并重新评审。

### P-1：因子库（Q-013）

五类因子，**按信息源分类**——扩充因子库的判据是引入新信息源，不是新增指标（D-3）。

| 因子 | 信息源 | 定义 | 参数 |
|---|---|---|---|
| `momentum` | 价格历史 | 过去 `N` 根 K 线的累计对数收益 | `N ∈ {5, 20, 60}` 逐代理抽取 |
| `reversion` | 价格历史 | `(anchor − last) / last`，`anchor` 为内生 EWMA | 半衰期 τ 逐代理抽取（对数正态） |
| `herding` | 成交流 | `(主动买量 − 主动卖量) / 总成交量`，窗口 `W` 根 K 线 | `W ∈ {1, 5}` |
| `book` | 订单簿状态 | `(买方 k 档深度 − 卖方 k 档深度) / 两侧之和` | `k = ±10 tick`（MD-003） |
| `noise` | 无 | 每次决策独立抽取的标准正态 | — |

`momentum` 与 `reversion` 同属价格历史但**方向相反**，两者对抗是厚尾与波动聚集的
必要成分。MACD、均线交叉等指标不单独入库——它们是 `momentum` 的不同滤波形式，
入库只会制造共线（D-3）。

**须实测校准**：因子相关矩阵随每次研究运行输出；若任意两因子的跨代理信号相关系数
持续超过 0.8，说明多样性为假，须调整因子定义并重新评审。

### P-2：保证金参数（Q-014）

量级参考真实市场实践，作为参数扫描的**中心点**——A-005 要求它们必须作为扫描维度，
而非固定值。

口径为**保证金率**（保证金/名义价值），不是担保比例（权益/保证金占用）——后者在
无仓位时分母为 0（[账户合同](../../contracts/margin-and-account.md) §3.1）。

| 参数 | 取值 | 依据 |
|---|---|---|
| `maint_bp` 维持保证金率 | **500**（5%） | 主流加密永续维持保证金率量级 |
| `target_bp` 强平恢复目标 | **1000**（10%） | 高于维持线，避免立即二次触发 |
| `initial_bp` 初始保证金率 | `ceil(10000 / leverage_tier)` | 由杠杆上限推出；向上取整确保不超档（账户合同 §3.1.1） |
| 杠杆上限档位 | 1x / 3x / 10x | 逐代理抽取；与 `leverage_tier_distribution` 一致 |
| 上限分布 | 60% 只能 1x、30% 最高 3x、10% 最高 10x | 多数账户安全、少数脆弱，连锁才有触发条件 |
| `grace_ns` | **0（v0.1 强制，配置校验拒绝非零值）** | 见下方「为什么强制为 0」 |
| `liquidation_latency_ns` | **1,000,000**（1 ms） | 风控判定到强平单入簿；不得为 0（事件 Schema §1.2） |
| `funding_rate_bp` | **0** | 第一版不启用；无现货参照，费率无从确定（账户合同 §6） |

保证金率一律以**整数万分数**表达（5% 记为 500），避免浮点比较决定强平与否
（指标字典 §4.1）。

**初始仓位恒为 0**，因此不存在「初始杠杆分布」——上表分布决定的是各代理**能开到
多大**，实际杠杆由内生建仓产生（账户合同 §7）。

#### 为什么 `grace_ns` 在 v0.1 强制为 0

此前 `grace_ns` 是「保留的配置项」，但**非零取值在当前合同下不可实现**：调度合同
只定义了 `MARGIN_CALL → ORDER_ARRIVAL` 的 `liquidation_latency_ns`，没有定义宽限期
如何与它组合，也没有宽限到期事件；宽限期内权益恢复时是否撤销强平、何时重新检查、
重复跌破是否重置计时——全部未定义。**一个只能取 0 的配置项，不如明确写成 0。**

因此 v0.1：

- 配置校验**拒绝非零 `grace_ns`**，不静默忽略；
- 保证金率跌破维持线后**立即**进入 `PENDING_LIQUIDATION` 并调度强平单，
  只跨越 `liquidation_latency_ns`。

**这不是能力缺失，而是研究上的合理简化**：宽限期的作用是给账户主动补保证金的窗口，
而本模型中没有任何代理会补保证金（初始仓位为 0、无外部注资通道），宽限因此退化为
纯延迟——而纯延迟已由 `liquidation_latency_ns` 表达，那才是有研究意义的参数
（风控反应越慢，连锁滑落越深）。

非零宽限期需要一个新的**队列事件类型**（grace-expiry）及其同时间戳优先级，属于对
冻结的 class 清单的破坏性变更。若将来确有研究需要，须以 ADR 引入并提升
`schema_version`，同时定义恢复撤销规则与重复跌破的计时语义。

**须实测校准**：若首次运行中强平从未触发或全市场瞬间清空，说明分布或阈值偏离可
观察范围，须调整并重新评审——两种极端都使 US-4 无法回答。

### P-3：订单流预知者的可见范围（Q-015）

**仅可见未来 `N` 个事件内、数量超过阈值 `Q` 的订单**，`N` 与 `Q` 为显式配置参数。

不可见一段时间窗内的全部待处理订单——那等于全知，会使 A-002 在实现层被架空，且
「信息优势」不再是可测量的连续维度。该定义对应真实市场中的 mempool 可见性与大单
探测，`N` 与 `Q` 共同构成信息维度的强度，可单独扫描（KPI-008）。

**本节随订单流预知者一并后移至 v0.2+**，恢复前须先解决「未闭合事项」中记录的因果自指
问题。第一版不实现预知者。

## 时间与随机性内核

见 ADR-002 与 D-5：

- **KR-001**：离散事件内核，不使用固定步长。
- **KR-002**：整数逻辑时间戳（纳秒），禁止浮点时间。
- **KR-003**：队列调度键为 `(timestamp, priority_class, enqueue_seq)`；日志与重放全序键为
  `(timestamp, transaction_seq, record_index)`。订单时间优先以其到达事务的
  `transaction_seq` 判定。
- **KR-004**：每代理独立 RNG 流，由 **`hashlib.blake2b`**（标准库，KR-005）按语义键
  派生：`(master_seed, agent_id, mechanism, decision_index, draw_index)`。
  **不使用 `numpy.random.SeedSequence`**——NumPy 不是标准库。分布变换算法见
  [代理策略合同](../../contracts/agent-strategy.md) §10。
  仅按 `agent_id` 派生是不够的——处理变量改变代理行为后，抽取次数与顺序会分叉，
  「相同种子」不再等于「相同随机冲击路径」，配对对照的效力被悄悄削弱。语义键使
  同一决策序号上的同一机制抽到同一随机数，与另一组的行为差异无关。
  报告须区分**相同种子**与**相同随机冲击路径**两种说法。
- **KR-005**：核心领域层仅使用 Python 标准库。
- **KR-006**：事务中新入队事件的 `queue_key` 必须严格大于当前队列事件；事务记录不入队，
  与父事务共享 `transaction_seq` 并按 `record_index` 递增写入。跨 class 回退只允许
  ADR-002 白名单且必须跨越至少 1 ns，因此禁止零通信延迟。

## 研究与模型假设

- **A-001**：成交价格完全由订单交互形成，不存在外生基本价值过程。
- **A-002**：代理只能使用其信息集内的数据，不具有全知视角。
- **A-003**：任何参与者优势都能分解为资金、信息、速度或执行能力四个维度；「信息」
  由对未来订单流的提前可见性承载。
- **A-004**：内生锚含自指结构，其稳定性不得假定。锁定、发散与周期振荡三种失效形态
  必须通过参数扫描识别，可用区与失效区均须作为结果报告（D-4）。
- **A-005**：杠杆连锁的规模对维持保证金率与**杠杆上限分布**高度敏感，**这些参数必须
  作为扫描维度而非固定值**（D-2）。注意初始杠杆恒为 0，实际杠杆是内生建仓的结果，
  因此「杠杆水平」本身也是被观测量而非处理变量。

## 非功能需求

- **NFR-001**：相同代码、配置与种子产生逐事件一致的结果。
- **NFR-002**：订单簿、账本与强平逻辑的分支覆盖率不低于 90%。
- **NFR-003**：基准配置在参考硬件上的单核运行时间满足 `benchmarks/` 声明的门槛。
- **NFR-004**：核心领域层不依赖 UI、数据库或机器学习框架。

## 成功指标

- **SC-001**：撮合、优先级、部分成交、撤单、保证金与守恒测试全部通过。守恒断言为
  指标字典 §5 的两条等式——仓位恒等式 C1（`Σposition ≡ 0`）与价值守恒 C2
  （`Σ(wallet − entry_notional)` + 费用 **+** 风险账户 = 初始 `Σwallet`；风险账户为
  负值，加号才使核销抵消），
  **整数精确成立**。
  `Σwallet` 与 `Σentry_notional` **各自都不守恒**，守恒的是两者之差；测试**必须
  包含三代理跨价换手**，否则会误证已被推翻的旧等式（账户合同 §2.3）。
  个体账户另须桥接检验 `Δequity = Δwallet + Δunrealized`。
  **十个验收向量**（[`acceptance-vectors.md`](../../contracts/acceptance-vectors.md)）
  须全部通过，其整数期望值是实现的唯一裁判。
- **SC-002**：固定种子的两次运行产生相同事件摘要哈希。
- **SC-003**：单个命令可运行不少于 30 个种子的对照实验。
- **SC-004**：报告覆盖 FR-016 的全部指标，并给出 FR-017 的五项会计桥接，残差为 0。
- **SC-005**：基准市场验证报告逐项声明 PRD §12 统计特征为通过 / 失败 / 不适用，含
  发散样本判定标准与排除率，并报告**强平连锁规模分布**。
- **SC-006**：遍历日志中全部成交，因果链每一跳的目标事件唯一存在且 `log_key` 严格更小；
  账户分录与快照自洽。该检查不依赖重放。
- **SC-007**：每条信念检验结论以条件性命题表达，含效应量、置信区间与失效边界；
  每条能力维度归因由只改变该维度的配对对照支撑。
- **SC-008**：回放器仅以事件日志为输入还原整段仿真，与原运行逐帧一致。

## 边界情况

以下场景必须有明确定义的行为（FR-018）：

- 空订单簿上的市价单——含强平单无对手方的情形；
- 保证金率恰好等于维持线；
- 强平单部分成交后按新状态重新计算数量（账户合同 §4.3）；
- 强平单无对手方时的重试调度（退化状态 §3.5）；
- 空市场启动至首笔成交（代理策略 §3.2）；
- 连环强平——一次强平触发下一次的链式反应；
- 穿仓——权益为负后的账户状态与后续订单权限；
- 代理破产、价格趋近下界、订单簿单边清空、长时间无成交、价格发散。

## 未闭合事项

**实现合同已全部闭合**：产品决策（Q-001—Q-016）、
[代理策略合同](../../contracts/agent-strategy.md)（信息集→因子→信号→目标仓位
→意图→准入）与 PnL 会计桥接（指标字典 §5.2）均已写定。**0.1.1 可以开工。**

仍未闭合的是**预注册研究设计**（PRD §17.2 C），须在正式实验前完成，可与 0.1.1 并行
准备：0.1.2 验证协议的数值门槛与统计判定、旗舰实验的处理与终点定义、校准区/验证区/
实验区的划分。

### P-3 的已知缺陷（后移前记录）

订单流预知者后移至 v0.2+，但其定义存在**因果自指**，恢复时必须先解决：「未来 N 个事件
内的订单」中，有些订单是预知者自己行动后才会产生的——尤其强平单由 `MARGIN_CALL`
生成，而 `MARGIN_CALL` 依赖价格，价格又依赖预知者的行为。

可行方向：只允许看见**已提交、已进入通信队列、但尚未到达交易所**的订单意图
（`latency_ns > 0` 恰好制造了这个窗口）。这样可见集合与预知者的当前决策无关，自指
消解。P-3 中「强平单同样在可见范围内」一句须删除。

### 根规格 Stable 条件待 ADR-003 定案

[ADR-003](../../decisions/003-goal-driven-agents-and-flagship-identification.md)
（Proposed）指出当前目标仓位公式把 `leverage_tier` 直接写进了代理的目标仓位计算，
使旗舰问题（PRD §3.1：杠杆/保证金是否足以自发产生涌现式崩盘、暴涨或流动性枯竭）
的因果识别不成立——0.1.2/0.1.3 现有证据只验证了工程链路，不构成旗舰结论。

若 ADR-003 转为 Accepted，本文头部「根规格转 Stable 的条件是 0.1.4 退出」须改为
新增里程碑（暂定 0.1.5：目标驱动代理与旗舰实验识别）退出，且该里程碑需求须先补入
下方追踪矩阵。若 ADR-003 被 Reject 或改写，本节随之更新或删除。**在此之前，0.1.4
退出不得被解读为 v0.1 已完整签收。**

## 需求追踪矩阵（唯一真源）

**规范真源是 [`traceability.json`](traceability.json)，不是下表。** 下表供人阅读，
由 JSON 生成或与之比对；**T607 只解析 JSON**。

理由与 `event_fields.json` 相同：范围（`FR-005—FR-008`）、复合 owner、阶段切片、
带/不带里程碑前缀的 exit ID——这些人类写法没有可判定的 grammar，让实现者「实现时
一并设计」等于让他同时设计验收输入和让自己通过的解析器。

JSON 的每条 requirement 恰有三种 `status` 之一：`owned`（含 `owners`：里程碑 +
退出条件 ID 列表）、`deferred`（含 `defer_to`）、`removed`。**`owned` 而 `owners`
为空即校验失败。**

**每个必选需求必须有唯一归属**，没有归属的需求视为文档缺陷。

本矩阵的存在是因为 FR-019/FR-020/SC-008 曾在 0.1.1 → 0.1.2 → 0.1.3 之间互相推诿，
三个里程碑可以全部签收而它们仍未交付（封板审计 P0-O03）。**自然语言的「或按需」
不构成归属。**

| 需求 | 归属里程碑 | 退出条件 |
|---|---|---|
| FR-001 撮合与订单簿 | 0.1.1 | E3 |
| FR-002 制度钩子 | 0.1.1（接口）/ v0.2（股票式实现） | E3 |
| FR-003 计费与对齐 | 0.1.1 | E1、E2 |
| FR-004 账户与记账 | 0.1.1（结构）/ 0.1.2（保证金逻辑） | 0.1.1 E1、E2；0.1.2 E1 |
| FR-005—FR-008 杠杆与强平 | 0.1.2 | 0.1.2 E1、E2 |
| FR-009—FR-013 代理与信念 | 0.1.2 | 0.1.2 E3 |
| FR-014 批量运行 | 0.1.2 | 0.1.2 E3 |
| FR-015 事件日志与因果链 | 0.1.1（脚本化订单流）/ 0.1.2（代理观察—决策与强平链） | 0.1.1 E6；0.1.2 E7 |
| FR-016—FR-017 指标与 PnL 桥接 | 0.1.1（残差断言）/ 0.1.2（完整指标） | 0.1.1 E1；0.1.2 E4 |
| FR-018 退化状态 | 0.1.1（簿侧）/ 0.1.2（穿仓） | 0.1.1 E3；0.1.2 E1 |
| **FR-019 单文件 HTML 回放器** | **0.1.4** | 0.1.4 E1、E2 |
| **FR-020 K 线视图** | **0.1.4** | 0.1.4 E3 |
| KR-001—KR-003 时间内核与定序 | 0.1.1 | E5 |
| KR-004 分流 RNG | 0.1.1（均匀）/ 0.1.2（完整分布） | 0.1.1 E4；0.1.2 E3 |
| KR-005 核心层无第三方依赖 | 0.1.1 | E8 |
| KR-006 队列单调性 | 0.1.1 | E5 |
| NFR-001 逐事件一致 | 0.1.1 | E4 |
| NFR-002 覆盖率 | 各里程碑各自 | 0.1.1 E9；0.1.2 附加门槛 |
| NFR-003 性能 | 0.1.2（需代理产生负载） | 0.1.2 E5 |
| NFR-004 分层解耦 | 0.1.1（L1）/ 0.1.4（L4） | 0.1.1 E8；0.1.4 E5 |
| SC-001 撮合与守恒 | 0.1.1 | E1、E2、E3 |
| SC-002 确定性哈希 | 0.1.1 | E4 |
| SC-003—SC-004 | 0.1.2 | 0.1.2 E3、E5 |
| SC-005 退化状态可判定 | 0.1.2 | 0.1.2 E4 |
| SC-006 日志自包含 | 0.1.1（账户）/ **0.1.4（订单簿逐帧）** | 0.1.1 E6；0.1.4 E1 |
| SC-007 条件性结论 | 0.1.2 | 0.1.2 E6 |
| **SC-008 逐帧回放一致** | **0.1.4** | 0.1.4 E1 |

**明确后移（非 v0.1 范围）**：股票式制度（收盘、T+2、熔断）、大资金执行者、
订单流预知者（P-3，含因果自指缺陷）、人在环交互——全部 v0.2+，理由见 PRD §15。

**维护规则**：新增或移动需求时**先改本矩阵**，再改各里程碑文档。

**该规则由 `0.1.1 T607` 机器强制**，不是一句约定——一句无人执行的约定正是
FR-019/FR-020 当初能失去 owner 的原因。CI 解析
[`traceability.json`](traceability.json) 并校验四项：

1. 本矩阵的需求 ID 集合与本文各需求章节声明的集合**完全相等**（多一个少一个都失败）；
2. 每个归属里程碑的目录与 `spec.md` **真实存在**；
3. 每个引用的退出条件 ID 在该里程碑的退出条件表中**真实存在**；
4. **没有任何必选需求处于「无 owner」状态**——既不在矩阵中，也未列入明确后移。

负向夹具：临时删除矩阵中的 0.1.4 映射，CI **必须失败**。只做正向检查无法证明它
真的在挡东西。
