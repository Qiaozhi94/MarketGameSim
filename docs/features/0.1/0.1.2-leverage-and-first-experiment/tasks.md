# 0.1.2：杠杆与第一个实验闭环 · 任务拆解

**对应里程碑**：[`spec.md`](spec.md)（状态见其 frontmatter）  

## 约定

- 每个任务标注 `[合同引用]`；实现前先读对应章节，实现后以合同和验收向量为裁判；
- 带 `[TDD]` 的任务先写失败测试；同一 Phase 内可并行项标 `[P]`；
- 研究协议、排除规则和主要终点必须在读取实验结果前冻结；
- 0.1.2 不修改 0.1.1 的事件、撮合和账本语义。若发现合同缺陷，按 0.1.1 的“遇到合同缺陷时”
  流程先修文档和黄金向量；
- **任务编号只在本文件内唯一**。引用其他里程碑的任务时必须带里程碑前缀
  （写 `0.1.1 T604`，不写 `T604`）——各里程碑的编号为 `T0xx`—`T7xx` 且**互相重复**，
  裸编号会指向错误任务。

---

## Phase 0：准入门与预注册

- [x] **T001** `[0.1.1 退出清单]` `[TDD]` 建立 0.1.2 启动门：自动验证 0.1.1 的
      **E1—E11 及 E5b、E5c、E6b**（共 14 项）证据均存在且通过；任一失败时 0.1.2 测试
      套件立即停止，不允许跳过。门的 ID 集合须由 0.1.1 `spec.md` 的退出条件表生成，
      不得手工抄写。
- [x] **T001b** `[0.1.1 E9]` 订单簿与账本分支覆盖率从 87% 补至 ≥90%：
      补撤单/准入拒绝/单边簿等路径的测试，使 `--cov-branch` 达到 90%，并将 CI 阈值从
      `--cov-fail-under=87` 提升至 `90`。同时更新 0.1.1 `spec.md` E9 与
      `tasks.md` T606/E9 的目标描述，去掉"（0.1.2 补至 ≥90%）"后缀。
- [x] **T002** `[PRD §17.2 C]` `[方法论 §9.3/§10.1]` 编写并冻结 0.1.2 基准市场
      验证协议：为厚尾、收益自相关、绝对收益自相关、冲击非线性、点差/深度与强平
      连锁分别声明统计量、通过区间、置信水平、多重比较方法、最低有效样本量和功效依据。
- [x] **T003** `[方法论 §10]` 编写旗舰实验预注册：可证伪问题、H0/H1、无杠杆与
      有杠杆处理、唯一主要终点、自我强化判据、次要指标、种子集合、排除标准、停止规则
      和否定条件。不得用"结果显著"作为停止规则。
- [x] **T004** `[方法论 §10.3]` 冻结校准区、验证区和实验区；配置校验禁止三个
      区域重叠，并在运行清单中记录分区版本与摘要哈希。
- [x] **T005** `[实验模板]` 建立实验 manifest：规格版本、Git commit、配置哈希、
      代码版本、Python/平台信息、随机种子、协议版本和产物路径。研究运行强制
      `information_set_mode=full` 与 `write_event_log=true`；须包含 T500b 产出的
      总采样点数、burn-in 剔除数与有效分析点数三个字段。
- [x] **T006** `[PRD §17.2 C]` 从旧 `EXP-000` 仅迁移仍适用的等价性检验、BH family、
      等价界与 bootstrap 口径；每一项都须重新映射到当前旗舰问题，不得整份照搬。
- [x] **T007** `[订单簿向量 OB-8/OB-9b]` `[账户 §4]` **合同冻结门（先于一切编码）**：
      补齐 OB-8 与 OB-9b 的**完整整数黄金值**——账户初态、费率、逐步计算过程、
      `postings`、事件顺序与终态；OB-8 须含安全 / 首次跌破 / 已 pending 且数量不变 /
      已 pending 且**数量重算** / 恢复 / 穿仓**六种账户**，验证 `m` = 可行动风险决定数
      而非扫描数。
      **另须四条向量**：① 部分强平成交后 `required_quantity_units` 重算（发生在强平单
      自己的事务内，`PENDING → PENDING` 仍必须写记录，且**换代 + 调度替代单**）；
      ② **延迟窗口内恢复**——强平单到达时账户已回到 `ACTIVE`，须
      `reject_reason=LIQUIDATION_STALE`；
      ③ **连续两次 q 变化、三张单乱序到达**——只有最新代次通过；
      ④ **三账户同批、三种 chain 归属**（续单继承 / 新拖入 +1 / 他链保留自身）。
      每条向量须逐事件给出 `liquidation_generation_after`、`chain_id`、`chain_depth`、
      旧单拒绝记录与替代单数量。
      **本任务的产出须经评审后冻结，T104/T201 只消费、不修改。**
      理由：由实现者在写代码的同时写黄金值，等于看到结果后反向选择「正确答案」，
      向量就失去了独立预言的作用——而它本来是设计阶段唯一能约束实现的东西。

## Phase 1：保证金准入与占用

- [x] **T101** `[账户 §3.1—§3.2]` `[TDD]` 实现 `initial_bp` 向上取整、维持保证金、
      `risk_equity`、风险名义价值和 `margin_ratio_bp` 向下取整；空仓为 `null`，不得以 0
      替代。
- [x] **T102** `[代理策略 §11.1]` `[撮合 §5]` `[TDD]` 在不可变订单簿快照上预撮合
      候选订单，以实际 maker 价、残余限价和 `risk_mark` 得到 `reservation_mark`；
      正式撮合必须与预撮合逐笔一致。
- [x] **T103** `[代理策略 §11.1]` `[ADR-001 §4]` `[TDD]` 整体重算
      `reserved_units = margin_part + fee_part`；手续费率取
      `max(taker_bps,maker_bps,0)`，每次挂单、撤单和部分成交后写
      `reserved_delta_units`。
      **`fee_part` 必须是两段之和**：`fee_immediate`（预撮合逐档立即成交量 × 该档
      maker 价）+ `fee_resting`（入簿剩余量 × `reservation_mark`）。
      **验收用例**：一张跨三档的 crossing order，只有少量残余入簿——若只按残余估费，
      冻结额接近 0 而实际立刻要付三档 taker 费。
- [x] **T104** `[账户 §3.3]` `[订单簿向量 OB-9b]` `[TDD]` 接通开仓准入：比较
      `reserved_after <= risk_equity`；减仓单不得因保证金不足被拒，拒绝时订单簿和账户
      必须零变化并写明稳定的 `reject_reason`。
      **消费 T007 已冻结的 OB-9b 黄金值，不得修改它**；其 `log_key` 结构须与 0.1.1
      已通过的 OB-9a 对齐：被拒事务只有 `record_index=0`。
- [x] **T105** `[验收向量 6/7b]` `[TDD]` 固化 3x 边界与四组 reserved 整数夹具，
      同时断言 `q/price/cash` 单位转换、before/after/delta 和手续费上界。

## Phase 2：强平、重试与穿仓

- [x] **T201** `[账户 §4.1]` `[撮合 §2.3]` `[订单簿向量 OB-8]` `[TDD]` 每个
      `ORDER_ARRIVAL` 事务完成全批结算后只运行一次两阶段扫描：先捕获零仓位负钱包，
      再按 `agent_id` 升序检查非零仓位保证金；批内不得提前触发。
      **消费 T007 已冻结的 OB-8 黄金值，不得修改它**。`m ≥ 2`；
      **`m` = 产生「可行动风险决定」的账户数**（事件 Schema §4.2.2）：保持 `ACTIVE`
      且充足、或保持 `PENDING_LIQUIDATION` 且 `required_quantity_units` **不变**的账户不产生记录；
      但 `PENDING → PENDING` 且**数量重算**时**必须**产生记录。
      `MARKET_DATA_PUBLISH` 的 `record_index` 为 `4+m`，且恒为事务最后一条记录。
- [x] **T202** `[账户 §4.1]` `[事件 Schema §4.2.2]` `[TDD]` 实现
      `ACTIVE ↔ PENDING_LIQUIDATION → LIQUIDATED` 状态机与 `liquidation_latency_ns`；
      **只有产生「可行动风险决定」的账户写 `MARGIN_CALL`**（事件 Schema §4.2.2），
      `verdict ∈ {OK, PENDING_LIQUIDATION, BREACHED}`（`LIQUIDATING` 已删除）。
      `OK` **仅用于 `PENDING → ACTIVE` 的恢复转移**，不得每次安全扫描都记一条——
      那会使日志量为 O(账户数 × 成交数)。
      **但 `PENDING → PENDING` 且 `required_quantity_units` 变化时必须记录**
      （账户合同 §4.3）——那是新的下单数量，是决定不是状态。判据是数量是否变化，
      不是状态是否变化。
      **强平状态机依赖 `grace_ns == 0`**（v0.1 强制立即调度强平单，无宽限窗口）——
      该检查已由 0.1.1 `T103`/`config/validator.py::_check_grace_ns` 实现并通过
      测试，本任务**复用**它，不重新实现；这里点名只是为了显式记录状态机设计对
      它的依赖。
- [x] **T202b** `[事件 Schema §4.2.2「恢复后的失效」]` `[TDD]` **强平代次**
      `liquidation_generation`（账户字段，初始 0）：**每个产生新强平动作的决定都 +1**
      ——① 进入 pending；② **`PENDING → PENDING` 且 `required_quantity_units` 变化**
      （调度**替代**单）；③ 恢复（不调度新单）。
      **第 ② 条不可漏**：不换代会让新旧两张单都通过验证，账户被过量强平。
      强平 `ORDER_ARRIVAL` 到达时重验「仍为 pending 且代次最新」，不满足即
      `accepted=false, reject_reason=LIQUIDATION_STALE`（事务只有 `r0`）。
      `MARGIN_CALL` 须写 `liquidation_generation_after`。
      **两条验收用例**：① 跌破 → 调度 → 窗口内他人成交使该账户恢复 → 强平单到达
      **必须被拒**；② **pending 期间连续两次 q 变化、三张单乱序到达** → 只有最新代次
      那张通过，另两张均 `LIQUIDATION_STALE`（代次只增不减，故与到达顺序无关）。
- [x] **T203** `[账户 §4.2]` `[TDD]` 以整数二分求最小强平量；把 taker 手续费纳入
      平仓后 `risk_equity`，并同时证明 `q` 可行、`q−1 step` 不可行。不得用闭式近似值
      直接下单。
- [x] **T204** `[FR-006—FR-008]` `[事件 Schema §4.2.2]` `[TDD]` 生成
      `origin=LIQUIDATION` 的市价 IOC 单：因果外键为
      **`decision_event_id → 触发它的 MARGIN_CALL`**，并携带 `liquidation_generation`
      与 `trigger_ratio_bp`。
      **不存在 `ORDER_ARRIVAL.trigger_event_id`，也不存在 `TRADE_SETTLE.chain_depth`**
      ——`chain_id`/`chain_depth` 只在 `MARGIN_CALL` 上，经 `decision_event_id` 回溯。
      不得给予队列优先权，不得转为 GTC。
- [x] **T205** `[账户 §4.3]` `[退化 §3.5]` `[TDD]` 部分强平成交后基于最新钱包、
      仓位、entry、手续费和 `risk_mark` 重新计算；无对手方时保持 PENDING，不自旋，只有
      后续成交改变风险价后才重评。
- [x] **T206** `[账户 §5]` `[事件 Schema §4.2.3]` `[TDD]` 穿仓两步核销：代理钱包
      归零、交易所风险账户记有符号负值；阶段 1 必须捕获 `position=0, wallet<0`，包括
      `−1 cash_unit`。
- [x] **T207** `[退化 §3.4—§3.6]` `[TDD]` 强平失败、连环强平和链深传播：不得限速、
      截断或设置最大深度；用因果外键验证每一层的来源。
- [x] **T208** `[验收向量 7—9]` `[TDD]` 案例 7、7b、8、9 全部通过，包括
      `q₁=288678`、部分成交后 `q₂=193271`、无流动性状态序列和 C2=507000 的穿仓重放。
- [x] **T209** `[NFR-002]` `[TDD]` 增加保证金/强平属性测试：多空对称、临界 bp、
      多账户同批触发、跨档批末扫描、部分成交和风险账户守恒；相关分支覆盖率 ≥ 90%。

## Phase 3：完整确定性随机分布

- [x] **T301** `[代理策略 §10.3.1—§10.3.4]` `[TDD]` 实现 Marsaglia polar、
      Marsaglia–Tsang Gamma 与 Dirichlet；所有子采样器返回
      `(value,next_draw_index)`，逐位通过合同黄金向量。
- [x] **T302** `[代理策略 §9—§10]` `[TDD]` 实现对数正态、均匀区间、离散万分数
      和标准正态截断；`ln/sqrt/exp` 使用精度 28 的 `Decimal`，禁止 `math` 浮点路径。
- [x] **T303** `[KR-004]` `[TDD]` 每个机制使用独立语义键；证明某个 Dirichlet 分量
      多次拒绝不会改变其他分量，处理组行为分叉也不会改变相同决策序号的共同随机冲击。
- [x] **T304** `[NFR-001]` `[TDD]` 在两个独立进程中复现全部分布黄金结果和边界
      量化结果；失败时报告首个不一致的语义键和 draw index。

## Phase 4：代理、因子与做市商

- [x] **T401** `[代理策略 §1/§3]` `[TDD]` 建立只读 `InformationSet` 与观察快照；代理只能
      读取其可见字段，缺失值、冷启动、单边簿及首笔成交前行为严格按合同退化。
      **含 K 线序列聚合**（`bar_ns=60s`，指标字典 §1.9）：周期内无成交时不跳过，
      `open=high=low=close=` 前一根 `close`、`volume=0`；只向代理暴露**已完成**的
      K 线，当前尚未收盘的一根不可见。**这是因子计算的内部依赖，不是 K 线视图**
      ——后者（回放器渲染）仍归 0.1.4，本任务不涉及任何 UI/展示逻辑。**边界向量**
      （指标字典 §1.9.1）：成交恰好落在 `(k+1) × bar_ns` 时必须计入 `bar k+1`
      而非 `bar k`；观察发生在同一边界时间戳时只能看到 `bar 0..k`，看不到刚被
      该边界成交打开的 `bar k+1`。
- [x] **T402** `[代理策略 §2—§4]` `[TDD]` 实现动量、回归、从众、盘口和随机五因子，
      固定尺度截断后按一次性 Dirichlet 权重合成整数信号；输出因子值、权重与相关矩阵。
- [x] **T403** `[代理策略 §5—§7]` `[TDD]` 信号 → 目标仓位 → 订单意图：向零取整、
      `max_order_qty`、限价激进度、tick/quantity 对齐、全撤重报和非零通信延迟。
- [ ] **T404** `[代理策略 §11]` `[TDD]` 将代理意图接入预冻结、准入、自成交阻止与
      正式撮合；被拒订单不得污染下一决策的信息集或随机索引。
- [ ] **T405** `[代理策略 §8]` `[TDD]` 实现库存型做市商：库存偏斜、双边报价、
      `max_inventory`、全撤重报；其优势只来自显式资金/速度/执行配置，不读取未来订单流。
- [ ] **T406** `[事件 Schema §4.5—§4.6]` `[TDD]` 调度观察与决策，完整记录
      `AGENT_OBSERVE`/`AGENT_DECIDE` 的信息集、因子、信号、意图与因果外键；研究模式
      必须能机器验证成交 → 订单 → 决策 → 观察链。
- [ ] **T407** `[FR-009—FR-013]` `[TDD]` 固化代理构建测试：初始仓位恒为 0、
      权重一次运行内固定、能力与信念分离、不同代理 RNG 流互不干扰。**端到端冷启动
      验收**（代理策略 §3.2）：从空订单簿起，固定断言事件序列——做市商首次观察 →
      双边挂单 → 盘口出现 → 信念代理观察/决策 → 首笔成交 → `risk_mark` 从
      `initial_price` 永久切换到 `last`；调度顺序错误导致市场开不了张时本测试必须
      失败，不得依赖其余单元测试间接捕获。证据绑定 E3。

## Phase 5：指标、样本分类与报告

- [x] **T500b** `[代理策略 §3.4]` `[TDD]` burn-in 剔除与最短运行长度：按
      `max(N_max+1, W_max)` 根 K 线计算 burn-in（当前配置 61 根 ≈ 3660 秒逻辑
      时间）；统计检验前剔除该窗口内的采样点，不得混入厚尾/自相关/波动聚集等
      检验。运行元数据须分别记录总采样点数、burn-in 剔除数、有效分析点数三者
      （T005 的实验 manifest 消费这三个字段）。研究运行前置检查须拒绝
      「预期总时长 < burn_in + 2000 个有效采样点」（当前配置下 < 5660 秒逻辑
      时间）的配置——此前指标字典单独要求的「≥2000 采样点」比 burn-in 本身还短，
      运行可能在冷启动尚未结束时就满足终止条件，这条检查就是补这个洞。**边界
      向量**（指标字典 §2）：`t = 3660` 秒的采样点是首个有效点并保留，
      `t = 3659` 秒（burn-in 内最后一秒）必须被剔除。
- [x] **T501** `[指标字典 §2—§4]` `[P]` `[TDD]` 实现固定间隔采样和市场/代理时间
      序列：收益、波动、点差、深度、成交量、撤单率、冲击、权益、仓位、杠杆和交易成本。
      **必须实现 §4.2 的有效域规则**：`权益 ≤ 0` 时杠杆率为 null、峰值 ≤ 0 时最大
      回撤为 null、`mid_before` 未定义时冲击与滑点**排除该笔**（不是写 null）。
      **「钱包」直接取 `wallet_units`，不得加 `reserved_units`**——后者是占用标记，
      相加会使总资金凭空膨胀并与 C2 对不上。
      汇总跳过 null 并报告有效样本量；null 比例 > 30% 的量标注为不可采信。
      **边界向量**（指标字典 §2）：采样点 `t` 有成交时直接取该笔价格，无成交时
      前值填充须取 `timestamp ≤ t` 中时间戳最大的一笔，不得误取 `t` 之后的成交。
- [x] **T502** `[指标字典 §4.1]` `[事件 Schema §4.2.2]` `[P]` 输出强平触发数、
      强平成交量占比、`chain_depth` 分布、每条链规模与穿仓金额；普通成交与强平成交
      不得混计。
      **「每条链规模」按 `chain_id` 分组**，不能只用 `chain_depth`——同一批扫描里
      续单重算（继承）、新拖入（+1）、他链重算（保留自身）三种账户可能深度相同却
      不属于同一条链。归属规则见事件 Schema §4.2.2 的三条角色规则。
- [x] **T503** `[指标字典 §5.2]` `[TDD]` 对每个代理逐事件计算 Spread、Impact、
      Revaluation、Fees、Funding 五项 PnL 桥接，使用 `valuation_mark`，残差整数精确为 0。
- [x] **T504** `[退化 §4.0—§4.3]` `[TDD]` 样本分类器分离技术无效与经济终点；
      崩盘、穿仓、连锁清空判据取自预注册，不得把经济终点当作失败样本排除。
- [x] **T505** `[PRD PR-021]` 生成两部分报告：经济终点发生率/严重度，以及条件于
      未发生终点的连续指标；同时报告技术无效率、排除率和调整后的有效样本量。
- [x] **T506** `[KPI-006]` `[TDD]` 扩展独立日志验证器，对每笔成交的 maker/taker
      两侧订单分别按 `origin` 校验（事件 Schema §5.1—§5.2）：`AGENT` 来源验证完整
      「观察—信念—决策—订单—成交—账户」链，`LIQUIDATION` 来源验证「风控决定
      （MARGIN_CALL）—订单—成交—账户」链；不依赖回放器或内核对象。
- [x] **T507** `[plan §1—§2]` 建立可选依赖组：Hypothesis 只属于测试环境，Parquet
      写入依赖只属于分析/报告层；复用 **`0.1.1 T604`** 的导入边界检查（**不是**本文件
      的 T604，那是统计检验），保证 L1 核心层仍只有 Python 标准库。

## Phase 6：批量实验与条件性结论

- [x] **T601** `[FR-014]` `[TDD]` 实现实验运行器：多种子、处理/对照配对、失败
      隔离、可恢复执行和稳定排序汇总；运行 ID 由协议、配置、种子和代码版本确定。
- [x] **T602** `[方法论 §9.2/§10.5]` `[TDD]` 配对组只允许改变预注册的杠杆处理；
      验证其余规范化配置逐字节相同，并验证共同语义键随机冲击逐位相同。
- [x] **T603** `[方法论 §9.3]` 执行顺序固定为校准区 → 冻结验证区 → 信念实验区；
      进入验证区后校准配置只读，尝试修改即终止并留下审计记录。
- [x] **T604** `[预注册协议]` 实现预先声明的效应量、置信区间、bootstrap/等价性
      检验与多重比较校正；不得自动选择“最好看”的统计口径。
- [x] **T605** `[KPI-007/KPI-010/KPI-011]` 生成条件性结论：写明结构 S、参数区间 R、
      种子数 N、效应量、区间估计、失效/否定条件；显式声明零和恒等式不是发现，禁止
      单路径结论和真实市场外推。
- [x] **T606** `[KPI-005]` 按预注册门槛生成市场验证矩阵，每项只能为通过/失败/
      不适用并附证据；达到“够用即止”门槛后不得根据旗舰结果回头放宽标准。

## Phase 7：性能、确定性与里程碑验收

- [x] **T701** `[benchmarks README §1]` `[TDD]` 实现 BENCH-001 覆盖断言：至少一次
      强平、一次 `chain_depth>=1`、一次部分成交、一次撤单和一次单边簿事件；任一缺失则
      基准无效。（第17轮：`bench/coverage.py`，此前`tests/simulation/`为空目录，从未
      真正实现过；第18轮：五项断言在真实100k事务运行中全部实测通过，见E5）
- [x] **T702** `[benchmarks README §2]` 实现 CALIB-001、归一化耗时、事务吞吐量（`transactions_per_second` 为主指标，
      `event_records_per_second` 为辅助）与
      `book_operations` 计数；首次校准后冻结 `max_transactions` 和 golden，变更须说明原因。
      （第17轮：`bench/calib.py`+`book/orderbook.py::operation_count`已实现并测试；
      第19轮：用户提供CPU-Z测得的P核物理编号0-5，`tools/formal_calibration.py`
      锁定对应逻辑处理器0-11（掩码0xFFF）跑通正式标定——首次尝试因后台程序占用
      P核线程测得异常值（24.9秒中位数），关闭无关程序后重跑恢复正常（6.11秒中位数，
      与不锁核时一致），确认原因是线程竞争而非核心映射错误；`book_operations_golden`
      已写入`BENCH-001.yaml`（29310，5次运行一致）并冻结）
- [x] **T703** `[KPI-004/NFR-003]` 在参考口径验证归一化耗时 ≤ 10 秒，并记录同机
      相对回归；不得通过关闭强平或减少覆盖路径来换取性能。（第19轮：正式标定
      CALIB-001参考耗时0.195秒、BENCH-001归一化耗时6.11秒（均为5次中位数），
      写入`reference-machine.md`；6.11秒远低于10秒门槛，且是`--calibrated-coverage`
      模式下含全部覆盖路径的真实测量，未通过关闭强平/减少覆盖路径取巧）
- [x] **T704** `[NFR-001/SC-002]` `[TDD]` 同一研究 manifest 完整重跑两次，事件摘要、
      样本分类、聚合表和报告数据逐字节一致；墙钟字段排除在确定性比较之外。（第17轮：
      `tools/determinism_probe.py`+两个不同`PYTHONHASHSEED`的真实子进程比较，此前
      CI注释自己承认"由0.1.1 T602/退出条件E4负责"但从未实现）
- [x] **T705** `[NFR-002]` 运行完整测试与覆盖率门槛，归档 0.1.2 E1—E7 的机器可读
      证据索引。（第17轮：`docs/experiments/0.1.2-exit-evidence-index.json`，
      `pytest`1102 passed，book+ledger合并分支覆盖率93%）

---

## 退出检查清单

全部勾选后 0.1.2 完成，方可进入 0.1.3：

- [x] E1 十个验收案例全部通过（T105、T208）
- [x] E2 强平连锁三类指标完整输出（T207、T502）
- [x] E3 预注册实验可从配置哈希追溯到条件性结论（T003—T005、T601—T605）
- [x] E4 经济终点与条件连续指标两部分报告齐备（T504、T505）
- [x] E5 BENCH-001 性能和覆盖断言全部通过（T701—T703）——覆盖断言五项真实达标
      （第18轮：`--calibrated-coverage`真实100k事务运行强平=42、连锁强平=1、
      部分成交=76、撤单=27402、单边簿=76，`coverage_valid=True`，可重复确定性
      验证；过程中发现并修复真实bug：`_run_post_batch_risk_check`从未把
      `parent_chain_id`传给`run_phase2_margin_scan`，导致`chain_depth`在任何真实
      运行中都不可能超过0）；性能一侧第19轮完成正式标定（用户提供CPU-Z测得的
      P核物理编号，锁定对应逻辑处理器0-11），CALIB-001参考耗时0.195秒、BENCH-001
      归一化耗时6.11秒（均5次中位数），远低于10秒门槛，`book_operations_golden`
      已冻结（29310）
- [x] E6 KPI-005、KPI-007、KPI-010、KPI-011 达标（T605、T606）——排查发现
      KPI-011（零和恒等式显式声明）此前全仓库零实现，T605历史标记`[x]`时的
      声称与实际代码不符，已补齐`metrics/report.py::build_zero_sum_declaration`
      并接入`build_study_report`；随后跑一次真实配对研究运行（2做市商+20散户，
      leverage_tier 3x/10x单维度对照，5个随机种子，非BENCH-001基准/非人工预置
      杠杆账户）联合验证四项KPI：KPI-007条件性结论正确生成、KPI-010用5个种子
      非单路径、KPI-005六项逐一声明（本次规模下诚实declare为NOT_APPLICABLE，
      是KPI-005认可的三种声明之一）、KPI-011零和恒等式在10次真实运行上残差
      全部为0。详见`docs/experiments/0.1.2-e6-demonstration-run.md`
- [x] **E7** KPI-006 完整因果链机器验证，KPI-009 在真实研究运行下逐事件成立（T503、T506）
- [x] 附加门槛：保证金/强平覆盖 ≥ 90%，完整运行可确定重现（T209、T704—T705）

## 明确后移

- 单文件 HTML 回放器、逐帧 UI 与 K 线视图属于 **0.1.4**，不属于本清单；
- 替代行为映射、因子消融、参数空间稳健性和留出复核属于 0.1.3；
- 股票式制度、大资金执行者与订单流预知者属于 v0.2+。
