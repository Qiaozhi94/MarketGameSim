---
kind: milestone
id: 0.3.1
parent: v0.3-human-in-the-loop-crash-experiment
version: "0.3"
status: draft
research_claim_status: not-established
research_claim_required: true
evidence_class: formal-research
gate_version: 1
created: 2026-09-04
updated: 2026-09-06
prerequisites:
  - 0.2.1
---

# 0.3.1：H2 人在环崩盘反馈实验

> Owner: TBD | Target: v0.3

## 0. 来源与意图

- **版本规格**：[`../spec.md`](../spec.md)（US-301—US-303、FR-301—FR-306）。
- **PRD 来源**：[`../../../market-game-sim-prd.md`](../../../market-game-sim-prd.md) §15 H2。
- **架构来源**：[`../../../market-game-sim-architecture.md`](../../../market-game-sim-architecture.md)。
- **Research / Contract 来源**：[`methodology.md`](../../../research/methodology.md)、
  [`metrics-dictionary.md`](../../../research/metrics-dictionary.md)、
  [`event-schema.md`](../../../contracts/event-schema.md)、
  [`interactive-session.md`](../../../contracts/interactive-session.md)。
- **上游决策**：v0.2 已签收 H1；H1 产物与正式研究证据隔离。
- **功能类型**：research / runtime / workflow / validation / user-facing。
- **规格模式**：full。
- **变更类型**：ADDED。
- **一句话意图**：以不少于 158 个配对 seed block 检验冻结 AI 策略的内生崩盘机制，并让项目
  所有者通过 24 个 N-of-1 配对场景获得严格限定为个人、且不建立研究声明的体验与行为结论。
- **当前合同**：[`H2-dual-track-contract`](../../../experiments/H2-dual-track-contract.md)
  自 2026-09-06 起拥有参与者类型、样本单位、证据隔离与结论边界；与本文旧版真人招募
  细节冲突时以该合同为准。旧细节暂留作方向重置审计，不得用于实现或预算。

## 1. 问题、目标与非目标

### 问题

在无外生基本面冲击的合成永续市场中，冻结 AI 决策策略是否会在配对随机路径上改变价格
崩盘、流动性枯竭与强平连锁；项目所有者在完全匹配的场景中亲自决策时，相对两条 AI
反事实表现出怎样的个人行为和结果差异？两条轨道分别回答模型族机制与个人体验，不合并
样本量，也不推断人类总体。

Q-308 已裁决：**主要估计对象是匹配可观测接口与决策窗口后的参照策略差异**，不是“人类
接管 + 换目标 + 换节奏”的联合处理。主要对照是 `WINDOW_MATCHED_POLICY_CONTROL`——账户、
初始状态、信息集、动作空间和决策机会（同一有限窗口调度器）与参与者条件一致，主要预定
差异是决策由人还是由预注册策略产生。原方向性目标的 `GOAL_AGENT_CONTROL` 保留为
**次要描述性**对照，回答“相对于原方向性目标策略发生了什么”，不参与主要估计量的
多重性校正；它同样受有限窗口调度合同约束，不复用原始逐次调度。

这种匹配**不声称目标函数对齐**：策略的目标函数可精确写出，人类的不能——参与者只收到
文字任务与激励方案，实际执行的目标不可观测；v0.1 参照策略也不实现完整效用最大化。因此
本研究只匹配账户/风险边界、信息集、动作空间与决策机会，并把参与者任务/激励和参照策略
目标分别写入协议的可审计比较矩阵。控制条件是同代码、配置、种子和初始状态的纯代理反事实，
不是另一批人类受试者。

结论语法：正式结论一律表述为“在匹配账户、信息、动作与决策窗口下，人类决策相对指定
参照策略的差异效应”，
每个结论句必须同时限定参与者池、任务与激励方案、模型族。**禁止**把它简称为“人类效应”
或扩写为脱离协议与模型族的普遍结论；次要对照的结果必须另起段落并标注为描述性。

### 目标

本节旧版编号由双轨合同重新解释：正式研究主样本是不少于 158 个 AI paired-seed blocks
（下限来自 `H2-block-power-baseline-v1`，最终数由校准冻结，不得下调）；唯一
真人是项目所有者，完成 6 个训练和 24 个正式 N-of-1 paired blocks。T901 必须在开发前将
下列遗留“每个人类”措辞逐项重基线，不得直接实现旧招募合同。

1. 预先冻结主要假设、估计量、样本量/停止规则、分配、顺序、排除规则和分析计划。
2. 为每个人类正式会话生成同种子的窗口匹配参照策略反事实，并附一个
   保留原方向性目标的次要描述性反事实。
3. 分别估计人类决策相对主要参照策略对价格崩盘、流动性枯竭、强平连锁发生率和严重程度
   的双侧差异，并单独报告相对原方向性目标代理的描述性对比。
4. 预注册激进订单、流动性撤回和风险减仓指标，解释观察到的方向与时序。
5. 交付可复建、可审计、隐私最小化且明确限制的正式研究证据包。

### 非目标

- 不招募、筛选或支付任何外部真人，不使用招募平台。
- 不把项目所有者的一人结果外推为普通交易者、人类总体或真实市场规律。
- 不把同一 AI 模型的多次调用称为多个独立参与者；AI 正式推断单位是配对 seed block。

- 不施加外生价格、基本面、新闻或订单流冲击。
- 不把三类主要结果压缩成综合崩盘得分，不因显著性选择性报告结果。
- 不把相关的机制指标称作已识别的因果中介；因果中介需要独立操纵或额外假设。
- 不比较人类与代理的裸反应速度，不允许正式运行通过暂停获得无限思考时间。
- 不使用 H1 自由交互、培训、理解检查、pilot 或失败调试运行建立正式结论。
- 不外推到真实市场，不提供投资建议，不接触真实资金或账户。

## 2. 用户场景

### US-301：执行配对的人在环实验（Priority: P1）

作为研究者，我希望参与者在冻结任务和操作窗口内占用指定目标插槽，并为会话取得同种子的
预注册纯代理参照策略反事实，以便估计人类决策相对参照策略的差异效应，而不是比较不可比的
市场路径。

**为什么是这个优先级**：没有配对反事实和可审计比较矩阵，H2 的主要估计量没有识别基础。

**独立测试**：对固定协议与种子分别生成参照策略和人类条件 manifest，机器验证
比较矩阵中标为“相同”的字段逐字段一致、标为“不同”的字段完整披露，并拒绝未注册漂移。

**验收场景**：

1. Given 已冻结协议和合格参与者，when 启动正式 block，then 控制运行与处理运行共享
   配置、种子、初态、背景代理和逻辑时长。
2. Given 正式运行请求修改信息权限、窗口或制度参数，when 校验协议，then 会话在写入样本
   前 fail closed。

### US-302：审计人类行为机制（Priority: P1）

作为研究者，我希望将人类每次决策与盘口、成交、账户和强平因果链连接，以便判断主要
结果的变化是否与预注册行为机制一致。

**为什么是这个优先级**：项目要识别人类带来的反馈通道，而不只是报告两组终点均值。

**独立测试**：从人类决定事件回溯激进成交、撤单/改挂、深度变化、风险减仓和首次强平，
并验证指标只使用决定当时可见及随后发生的数据。

**验收场景**：

1. Given 一次人类正式会话，when 生成机制表，then 每个指标能追溯到规范输入和事件 ID。
2. Given 因果链缺失或指标定义未冻结，when 构建正式分析，then 样本被拒绝而非补猜。

### US-303：生成边界清晰的正式结论（Priority: P1）

作为审阅者，我希望独立查看三个结果家族的效应量、不确定性、排除项和限制，以便判断
数据支持放大、稳定还是未检出差异。

**为什么是这个优先级**：双侧假设允许任一方向或未检出；报告不能把“没有显著差异”包装
成等效，也不能用综合指标隐藏相反方向。

**独立测试**：从冻结 evidence index 重建分析，核对样本流图、配对完整性、三类结果、
机制分析和结论措辞。

**验收场景**：

1. Given 合格正式样本，when 生成报告，then 三类结果分别呈现配对效应和不确定性。
2. Given 数据不足或区间跨越预注册实质阈值，when 生成结论，then 报告“未建立方向性差异”
   而非声称无效应。

## 3. 范围与边界

### 范围内

- 单品种、合成加密式永续市场；无外生基本面信息或价格冲击。
- `HUMAN_REPLACEMENT`、`WINDOW_MATCHED_POLICY_CONTROL`（主要对照）与 `GOAL_AGENT_CONTROL`
  （次要描述性对照）三条件的同种子配对 block；两条对照均为纯代理运行，可预生成。
- 一个真实参与者替换一个预先指定的目标插槽；其余代理、制度和初始状态冻结。
- 两条纯代理对照均必须运行在与参与者相同的有限窗口调度器下（同窗口长度、每窗至多一个动作、
  超时同样写 `NO_ACTION`）。窗口匹配参照策略取自 v0.1 已验证的代理家族并随协议预注册，
  不得为 H2 重新调参；`GOAL_AGENT_CONTROL` 也必须使用同一有限窗口调度器，只保留原方向性
  目标策略差异。
- 固定任务说明、信息权限、操作窗口、训练与理解检查、场景顺序分配。
- 价格崩盘、流动性枯竭、强平连锁三个结果家族及预注册机制指标。
- 匿名参与者 ID、同意/退出记录、系统故障和预注册排除状态。
- preview 与正式运行隔离、正式分析、报告、代表性回放和 evidence index。

### 范围外

- H1 `interactive` 自由操作数据和事后挑选的演示路径。
- 多个人类同时交易、参与者互相通信、排行榜和真钱激励系统。
- 外生新闻、基本面、价格注入或专门构造的压力冲击。
- 股票式制度、多品种、订单流预知、大资金执行算法和策略在线学习。
- 参与者人口统计差异、专业水平异质性或人格特质的验证性推断；除非另立预注册研究。

### 边界场景

- 参与者断线、客户端崩溃或运行完整性失败时，按冻结规则标记技术无效；不得根据结果决定
  是否排除，也不得事后补跑同一 block 而不留记录。
- 参与者撤回同意时，其数据按冻结同意与保留政策处理；身份映射不进入项目 artifact。
- 参与者超时按协议定义的 `NO_ACTION` 处理，不暂停市场，也不由实验员代替决策。
- 配对任一成员缺失时，该 pair 不进入主要配对分析；缺失数量与原因完整报告。
- 若主要分析发现方向变化，机制指标只能支持“与某行为通道一致”的解释，不能单独证明
  中介因果关系。
- 若三个家族方向不一致，逐项报告，不选择最符合故事的一项作为总体结论。

## 4. 需求

> **方向重置门**：本节至 §7 尚含旧版多人真人合同，只作为变更审计输入，不是可执行需求。
> T901 必须依据 `H2-dual-track-contract-v1` 完成逐项重基线并通过合同测试后，里程碑才可从
> `draft` 推进到 `ready-for-development`；在此之前禁止实现旧 `HUMAN_REPLACEMENT`、招募、
> 报酬、身份映射或人群推断路径。

### 功能需求

### Requirement: 冻结 H2 协议与配对反事实（`FR-301`）

系统应在首个正式样本前冻结并内容寻址：研究问题、双侧假设、主要/次要估计量、三个结果
家族、机制指标、样本量与停止规则、目标代理、任务与激励、信息权限、操作窗口、训练、
理解检查、场景/顺序分配、排除/缺失规则、分析方法和结论语法。协议还必须冻结主要对照的
策略来源标识与版本、窗口调度匹配参数、参与者任务/激励与参照策略目标的比较矩阵，以及
次要对照的描述性定位。比较矩阵必须逐项声明账户/风险边界、信息集、动作空间和决策机会
是否相同，并明确目标函数不声称对齐。每个正式人类运行应绑定两个纯代理反事实：一个
`WINDOW_MATCHED_POLICY_CONTROL`（除决策生产者及其内部策略外的比较矩阵字段相同，含窗口
调度）与一个 `GOAL_AGENT_CONTROL`（保留原方向性目标策略，但使用同一窗口调度合同）。
`WINDOW_MATCHED_POLICY_CONTROL` 与 `GOAL_AGENT_CONTROL` 的策略 ID 或参数组合必须不同；协议须
记录主对照选择依据，说明为什么该策略适合作为主要参照，而不是按结果择优选择。
主对照固定为 `risk_budget_threshold_v1(theta_in=3000, theta_out=1200, k_x1000=600)`；次要
`GOAL_AGENT_CONTROL` 固定为 `risk_budget_linear_v1`。两者沿用 v0.1 已冻结的风险偏好抽样、
订单参数与同一非做市目标插槽，不得为 H2 调参或在场景间交换身份。

#### Scenario: 拒绝不完整或漂移协议

- GIVEN 协议缺失任一冻结项、比较矩阵中标为“相同”的字段发生漂移，或标为“不同”的字段未完整披露
- WHEN 请求进入正式样本
- THEN 系统以稳定原因拒绝且不写 evidence index

### Requirement: 运行受控的人类替换会话（`FR-302`）

系统应提供 `HUMAN_REPLACEMENT` 条件：人类仅通过与代理相同的订单、撮合、保证金和强平
路径替换指定代理，在冻结信息集和有限操作窗口内决策；背景市场继续按逻辑时间推进，
正式会话不得暂停、单步或临时改参。

#### Scenario: 操作窗口超时

- GIVEN 正式会话的一个决策窗口结束且没有合法输入
- WHEN 调度器提交该窗口
- THEN 写入预定义 `NO_ACTION`，市场继续推进且不等待墙钟补录

### Requirement: 隔离培训、预览与正式样本（`FR-303`）

系统应区分 `training`、`preview`、`formal` 样本阶段。只有绑定冻结协议、满足完整性规则且
未命中预注册排除条件的 `run_mode = human-experiment, sample_stage = formal` 配对运行可
进入 H2 正式分析；H1 `interactive` 及其它阶段必须 fail closed。

#### Scenario: H1 数据伪装成正式样本

- GIVEN header 或 manifest 表明运行源自 `interactive` 或非正式阶段
- WHEN 正式证据入口消费该运行
- THEN 原子拒绝且不生成部分统计、报告或索引

### Requirement: 分别计算三个结果家族（`FR-304`）

分析应按冻结指标口径，分别计算价格崩盘、流动性枯竭和强平连锁的发生与严重程度，并按
参与者内重复测量/配对 seed block 估计 `HUMAN_REPLACEMENT - WINDOW_MATCHED_POLICY_CONTROL` 的
双侧效应量与不确定性，这是唯一的主要估计量。多重性、缺失和实质阈值必须按冻结计划处理；
Holm 校正只作用于三个主要终点。`HUMAN_REPLACEMENT - GOAL_AGENT_CONTROL` 与
`WINDOW_MATCHED_POLICY_CONTROL - GOAL_AGENT_CONTROL` 同样计算并报告，但标为描述性，不参与
主要多重性校正，也不得替代主要结论。

三个家族各以发生概率的配对风险差为主要 estimand，最小实质效应为绝对 `0.125`；严重程度
均为次要指标。价格崩盘和流动性枯竭沿用 v0.1 的 EV-1—EV-4 归属、`ln(10)` 与
`5%*run_total_ns` 阈值；强平连锁以同一 `chain_id` 至少两个不同账户且
`max(chain_depth)>=1` 为发生条件。精确公式和零值/缺失语义见 design §9，并须在正式协议
冻结前写入指标字典。

#### Scenario: 结果方向不一致

- GIVEN 三个结果家族产生不同方向或不确定性
- WHEN 生成正式报告
- THEN 逐家族原样呈现，不生成未预注册的综合分数

### Requirement: 记录并分析预注册机制指标（`FR-305`）

系统应从可审计事件链计算至少三类人类行为：激进订单、流动性撤回、风险减仓；可附加
撤单/改挂、首次强平触发和终局权益。机制分析应预先声明时序与统计口径，默认解释为关联
和时序一致性，不声明因果中介。指标 ID、公式、单位、窗口、缺失语义和适用事件版本必须
写入 [`metrics-dictionary.md`](../../../research/metrics-dictionary.md)；冻结协议只引用其版本
与指标 ID，不得在实现、分析脚本或报告中另行定义同名口径。

#### Scenario: 指标无法追溯

- GIVEN 某机制值缺少规范输入、决定事件或后续市场事件引用
- WHEN 构建机制数据集
- THEN 将其标记为缺失并按冻结规则处理，不推断或手工补值

### Requirement: 生成 H2 正式证据包（`FR-306`）

系统应从冻结协议和 evidence index 单命令生成样本流图、配对检查、三类主要结果、机制
分析、敏感性分析、限制说明、代表性回放、机器可读结果和 manifest。报告结论必须区分
放大、稳定、未建立方向性差异，不把未拒绝零假设表述为等效。

#### Scenario: 重建正式结果

- GIVEN 发布的协议、evidence index 与代码版本
- WHEN 在新进程运行重建入口
- THEN 得到内容哈希一致的机器结果和语义等价的报告

### 数据 / 实体需求

- **DR-301**：`Protocol` 应保存协议 ID、内容哈希、冻结时间与所有预注册字段；
  `Participant` 只保存研究假名和资格/同意状态；`Assignment` 保存匿名参与者、seed block、
  场景顺序与条件；`ExperimentSession` 保存协议、分配、运行、阶段、完整性和排除状态。
  真实身份、联系方式与支付信息不得出现在事件日志、manifest、evidence index 或报告中。

### 事件 / Trace 需求

- **TR-301**：正式 `RUN_HEADER` 和 manifest 必须交叉绑定 `run_mode`、`sample_stage`、
  `protocol_id/hash`、匿名 `participant_id`、`assignment_id`、`pair_id`、条件、session order、
  目标代理和冻结窗口版本；H2 事件 schema 使用 v5，v4 只读回放且不得升级为正式证据，未知、
  缺失或不一致均 fail closed。
- **TR-302**：每个人类窗口必须写规范决定或 `NO_ACTION`，并通过 `input_seq`、`intent_id`、
  `decision_event_id` 连接订单、撤单、成交、账本、盘口变化与强平事件；墙钟仅作依从性诊断，
  不改变逻辑事件排序。

### API / 接口需求

- **IR-301**：实验入口应提供版本化的 enroll/consent、training/check、assign、run 和 withdraw
  操作；`run` 只接受已冻结协议签发的 assignment，客户端不得暴露 pause、step 或改参。
- **IR-302**：正式 evidence guard 应验证运行模式、样本阶段、协议哈希、pair 完整性、排除
  状态和 artifact 哈希；验证完成前不得写部分聚合或 evidence index。

### UX 需求

- **UX-301**：参与者界面必须显示合成市场/无真实资金、标准化任务、当前逻辑进度、可见
  信息、本人账户、决策倒计时、输入结果和退出入口，不显示其他代理私有状态或未来事件。
- **UX-302**：正式会话应覆盖 waiting、active-window、submitted、no-action、completed、
  technical-abort 和 withdrawn 状态；实验员不得通过界面替参与者提交市场动作。
- **UX-303**：训练与正式运行必须有持续可见的阶段标识，避免参与者误认训练行动进入样本。

### 非功能需求

- **NFR-301**：纯代理控制应在冻结代码/配置/种子下逐事件可复现；人类会话应由规范输入
  重放。配对字段比较和 artifact 哈希必须机器验证。
- **NFR-302**：收集人类数据前必须完成适用的伦理/同意确认；坚持数据最小化、假名化、
  可退出和冻结保留政策。成果包不得包含可直接识别个人的信息。
- **NFR-303**：停止、排除、补跑和缺失处理只能依据冻结规则；系统故障必须留下审计记录，
  不得根据已观察的市场结果改变样本纳入决定。

## 5. 生命周期与不变量

```text
DRAFT_PROTOCOL -> FROZEN_PROTOCOL  开放问题关闭 / 审查、preview、伦理与功效条件通过
FROZEN_PROTOCOL -> TRAINING        参与者同意且资格校验通过
TRAINING -> ELIGIBLE               理解检查按预注册规则通过
ELIGIBLE -> ASSIGNED               分配表签发 participant-seed blocks 与顺序
ASSIGNED -> RUNNING                正式客户端校验协议和 assignment
RUNNING -> COMPLETED               全部窗口与 trailer 完整
RUNNING -> TECHNICAL_ABORT         冻结技术无效条件命中
* -> WITHDRAWN                     参与者行使退出
COMPLETED -> INCLUDED/EXCLUDED     仅按预注册规则裁决并留审计证据
INCLUDED pairs -> ANALYZED         样本量/停止规则满足且 evidence index 冻结
```

不变量：

- 首个正式样本后协议不可原地修改；任何修订必须新建协议版本并按冻结规则决定是否重启研究。
- 每个主要分析 pair 恰含一个 `WINDOW_MATCHED_POLICY_CONTROL` 与一个 `HUMAN_REPLACEMENT`
  运行；账户、初始资金、风险边界、信息集、动作空间与窗口调度必须逐字段相同，允许差异仅为
  决策生产者、人类任务/激励与参照策略内部目标，并须在比较矩阵中显式列出。
- 次要 `GOAL_AGENT_CONTROL` 运行与主要 pair 共享 seed、配置和窗口调度参数，缺失时只影响
  描述性对比，不使主要 pair 失效。
- 正式市场中不存在外生基本面或价格冲击；人类输入是处理的一部分而非隐藏冲击变量。
- 样本阶段、运行模式和协议完整性缺一不可；`interactive` 永不升级为 `formal`。
- 三类主要结果保持独立；所有预注册结果均报告。
- 纳入、排除、停止和补跑决定不读取处理结果。
- 身份映射与研究 artifact 分离；撤回、保留和删除遵循冻结同意政策。

## 6. 成功与验收

### 成功标准

- **SC-301**：首个正式样本前存在内容寻址、机器可验证且覆盖 FR-301 全部字段的冻结协议。
- **SC-302**：达到预注册样本量/停止条件，所有纳入运行均有完整 pair、因果链和哈希校验。
- **SC-303**：价格崩盘、流动性枯竭、强平连锁及三类机制指标按冻结计划分别输出效应量、
  不确定性、缺失与敏感性结果。
- **SC-304**：独立新进程可从 evidence index 重建正式结果；报告明确条件性结论、隐私边界、
  排除项和不可外推范围。

### 退出条件

| 退出 | 条件 | 证据 |
|---|---|---|
| E1 | 协议、功效/停止规则、同意/隐私、排除与分析计划冻结并通过机器校验 | frozen protocol + review record |
| E2 | 锁定客户端完成训练与 preview；正式证据门拒绝 H1、非正式阶段和协议漂移 | experiment-preview bundle + guard matrix |
| E3 | 按停止规则取得完整正式 pair；重放、配对与纳入裁决通过 | formal evidence index + sample flow |
| E4 | 三个结果家族与机制分析按预注册方法重建，未产生综合分数或因果中介越界 | machine results + analysis checks |
| E5 | 正式报告、代表性回放、限制与 manifest 可复建；外部审阅通过 | H2 delivery bundle + review record |

### 验收清单

- [ ] **AC-301** (`FR-301`, `DR-301`, `NFR-302`, `NFR-303`, `SC-301`): 不完整协议不能冻结；
      冻结后内容变化产生新哈希且旧 assignment 不可复用。
- [ ] **AC-302** (`FR-302`, `IR-301`, `UX-301`, `UX-302`): 正式客户端在有限窗口内完成合法
      决策和 `NO_ACTION`，不暴露暂停、单步、改参或信息特权。
- [ ] **AC-303** (`FR-303`, `IR-302`, `TR-301`): evidence guard 原子拒绝 interactive、
      training、preview、未知模式、协议漂移和不完整 pair。
- [ ] **AC-304** (`FR-301`, `FR-302`, `TR-301`, `NFR-301`, `SC-302`): 每个纳入 pair 的
      比较矩阵中标为“相同”的字段逐字段一致（含账户、初始资金、信息集、动作空间与窗口
      调度参数）；决策生产者、参与者任务/激励与策略内部目标按矩阵分别披露。主对照可复现、
      处理可重放，次要对照缺失不使主要 pair 失效。
- [ ] **AC-305** (`FR-304`, `SC-303`): 三类结果分别输出发生率、严重程度、双侧配对效应、
      不确定性、多重性和缺失处理，不生成综合分数。
- [ ] **AC-306** (`FR-305`, `TR-302`, `SC-303`): 激进订单、流动性撤回和风险减仓指标均可
      追溯到事件链，并只作预注册的关联/时序解释。
- [ ] **AC-307** (`FR-306`, `IR-302`, `NFR-301`, `SC-304`): 新进程从 evidence index 重建
      机器结果和报告，且 artifact 哈希、样本流与结论语法通过校验。
- [ ] **AC-308** (`DR-301`, `NFR-302`, `UX-303`): 成果包不含直接身份信息，训练/正式阶段
      可辨识，撤回和保留行为符合冻结政策。

## 7. 测试、依赖与决策

### 测试策略

- 单元测试：协议冻结、assignment、窗口调度、配对比较、排除裁决、指标和结论语法。
- 集成测试：训练/正式会话、`NO_ACTION`、中止/撤回、重放、evidence guard 与原子输出。
- UI / E2E：有限窗口、阶段提示、信息闭集、无 pause/step/改参与参与者退出。
- 研究验证：冻结模拟数据恢复已知效应；preview 验证流程但永不进入正式证据；正式运行前
  完成分析代码盲测/锁定。
- 真实环境 / 手动验证：目标 Windows 环境训练与正式会话演练、断网与客户端故障恢复。

### 依赖

- 上游 Feature / Contract：已签收 v0.2.1；事件、代理、撮合、账户、指标与退化状态合同。
- 下游消费者：H2 分析、报告、回放、evidence index 和未来人类行为扩展研究。
- 外部 / 环境依赖：无外部真人招募；仅依赖项目所有者的 N-of-1 时间安排和冻结场景顺序。

### 决策与风险

| 决策 / 风险 | 结论或缓解 | 理由 | 后续 |
|---|---|---|---|
| 因果对象（Q-308 已裁决） | 主要估计量为匹配接口/窗口后的人类决策相对指定参照策略的差异；原目标代理对比降为次要描述性 | 机制分析（US-302、FR-305）需要一个预先指定、机会集可比的参照来源 | 主对照策略预注册且取自 v0.1 已验证家族，不为 H2 调参 |
| 匹配边界 | 匹配账户/风险边界、信息集、动作空间与决策机会；不声称人类任务与策略目标函数对齐 | 策略目标可写出，人类目标不可观测，v0.1 也不实现完整效用最大化 | 协议冻结比较矩阵；结论语法强制三限定词和参照策略名称 |
| 主要假设 | 双侧，允许放大、稳定或未建立方向差异 | 人类可撤回流动性，也可逆向供给或提前减仓 | 冻结实质阈值与区间解释 |
| 反事实 | 同 seed/config 配对纯代理运行 | 降低市场路径噪声并锁定矩阵声明的相同项与差异项 | 机器比较冻结字段并输出差异矩阵 |
| 机制声明 | 关联与时序一致性，不默认称因果中介 | 处理后的行为变量存在中介混杂 | 若需因果中介另立操纵实验 |
| 学习/疲劳 | 训练隔离，场景顺序随机/平衡，模型处理参与者内重复 | 人类会随会话学习并疲劳 | 协议冻结 block 数和休息规则 |
| 选择与排除 | 预注册、结果盲的纳入和补跑规则 | 防止事后选择有故事的运行 | evidence index 保留样本流 |

### 已裁决研究合同

| 问题 | 决策 | 状态边界 |
|---|---|---|
| Q-301 | 主对照为冻结参数的 `risk_budget_threshold_v1`；次要原目标代理为 `risk_budget_linear_v1`；全部场景保持同一非做市目标插槽和策略身份 | 已关闭 |
| Q-302 | 所有者任务为最大化正式场景平均终局权益并避免破产；外部招募、报酬和平台均为零 | 旧 Prolific/报酬合同已保留并标记 superseded |
| Q-303 | 每窗 `1_000_000_000ns` 逻辑时间/8 秒墙钟；每局 60 窗、每人 8 局、`4+4` block、休息至少 5 分钟；总会话目标 90 分钟、上限 105 分钟 | 已关闭；pilot 超时窗超过 10% 时只允许一次把墙钟窗增至 10 秒并重新冻结 |
| Q-304 | AI 正式轨固定 130 个配对 seed block；所有者轨固定 6 个训练和 24 个正式配对场景，分 4 天、每天 6 个 | 旧 130 名真人功效模型已废止；开发前须完成 paired-seed 方差校准 |
| Q-305 | 无外部资格筛选或身份映射；所有者可随时停止，技术故障只允许用预签发备用 seed 整局补跑 | 盈亏、破产和结果方向不得触发补跑 |
| Q-306 | 外部真人参与者为 0，唯一真人为项目所有者本人 | 无外部招募伦理/报酬流程；不得声称人群研究结论 |
| Q-307 | 崩盘与流动性枯竭沿用 v0.1 精确 EV/阈值；强平连锁新增 `chain_id`/`chain_depth` 判据；深度比例、峰谷回撤等只作次要指标；三个家族允许重叠且不合成 | 已关闭 |

## 8. 待确认问题

- [x] Q-301: 已关闭 — 决策：主对照采用冻结参数的 `risk_budget_threshold_v1`，次要原目标代理采用 `risk_budget_linear_v1`，全部场景固定身份和非做市目标插槽。
- [x] Q-302: 已关闭 — 决策：所有者执行平均终局权益任务；外部招募、报酬和平台均为零，旧预算证据保留但不再生效。
- [x] Q-303: 已关闭 — 决策：逻辑窗 1 秒、墙钟窗 8 秒、每局 60 窗、8 局按 `4+4` block、休息至少 5 分钟，总会话上限 105 分钟。
- [x] Q-304: 已关闭 — 决策：AI 正式轨固定 130 个 paired-seed blocks；所有者轨固定 6 个训练和 24 个正式 paired blocks；旧真人功效证据已废止，paired-seed 校准由 T902 跟踪。
- [x] Q-305: 已关闭 — 决策：不设外部资格筛选或身份映射；所有者可停止，只有技术故障可按预签发备用 seed 整局补跑。
- [x] Q-306: 已关闭 — 决策：外部真人为零、唯一真人为项目所有者；不运行外部招募、报酬或人群研究流程，不声称人群效应。
- [x] Q-307: 已关闭 — 决策：崩盘/流动性沿用 v0.1 EV 与阈值，强平连锁新增因果链判据，新增深度/回撤仅作次要指标，三家族不合成。
- [x] Q-308: 已裁决——主要估计对象是账户/风险边界/信息集/动作空间/决策机会匹配后，人类决策相对指定参照策略的差异（主对照 `WINDOW_MATCHED_POLICY_CONTROL`）；原方向性目标代理降为次要描述性对照 `GOAL_AGENT_CONTROL`。不声称目标函数对齐；结论语法、匹配矩阵与禁用简称见 §1、FR-301 与 §7。

