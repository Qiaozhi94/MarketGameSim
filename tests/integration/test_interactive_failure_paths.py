"""T814 skeleton: Windows, disconnect, write failure, and offline replay paths."""

import pytest


@pytest.mark.xfail(strict=True, reason="T814 failure paths are not implemented yet")
def test_interactive_failure_paths_contract():
    pytest.fail("T814 pending")
