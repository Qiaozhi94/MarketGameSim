# M1：最小确定性内核（无杠杆）

**父规格**：[`../spec.md`](../spec.md)　**架构**：[`../plan.md`](../plan.md)  
**状态**：Ready（实现合同已闭合，可开工）  
**创建日期**：2026-08-01

## 1. 目标与非目标

**目标**：建成一个**没有杠杆**的确定性市场内核——订单簿、账户、事件日志、确定性
回放全部跑通，且守恒与因果链在整数域精确成立。

**为什么先做无杠杆**：账户结构一次留好保证金字段（`reserved_units`、
`entry_notional_units`），但本阶段不开启杠杆。理由是把两类缺陷分开——带着杠杆调
账本，会让「撮合/记账错误」与「保证金/强平错误」混在一起，定位成本成倍上升。
接口一次设计对，实现分两步，这不等于「后期叠加」。

**本阶段不做**：

- 保证金准入检查、强平、穿仓核销（M2）；
- 信念因子、代理策略、做市商（M2）；
- 资金费（第一版恒为 0，机制预留）；
- 批量实验、统计、报告、回放器（M2/M3）。

**代理的替代**：M1 用**脚本化订单流**（固定的订单序列，来自验收向量与撮合用例）
驱动内核，不实现任何代理。这使内核可以在没有 L2 的情况下独立验收。

## 2. 范围（对应 001 的 FR）

| FR | 本阶段范围 | 备注 |
|---|---|---|
| FR-001 | **全部** | 订单簿、限价/市价单、撤单、部分成交 |
| FR-002 | 接口 + 加密式空实现 | 钩子调用点必须全部就位，实现可为空 |
| FR-003 | **全部** | tick/min_quantity 对齐、maker/taker 分离计费、最小单位整数 |
| FR-004 | 账户结构 + 记账 | 字段全部就位；**保证金逻辑留空** |
| FR-005—FR-008 | **不做** | 杠杆与强平，M2 |
| FR-009—FR-013 | **不做** | 代理与信念，M2 |
| FR-014 | 配置解析与序列化 | 批量运行留到 M2 |
| FR-015 | **全部** | 事件日志、因果外键、账户分录 |
| FR-016—FR-017 | 最小子集 | 仅 PnL 桥接的逐事件残差断言；报告层不做 |
| FR-018 | 部分 | 空簿、单边簿、无成交；穿仓相关留 M2 |
| FR-019—FR-020 | **不做** | 回放器与 K 线视图，M2 |

## 3. 退出条件

全部为**可执行断言**，缺一不可：

| # | 条件 | 判据来源 |
|---|---|---|
| E1 | C1（`Σposition ≡ 0`）与 C2（价值守恒）在**每个事件后**整数精确成立 | 指标字典 §5 |
| E2 | [验收向量](../../../docs/contracts/acceptance-vectors.md)案例 **1—5、10** 全部通过 | 案例 6—9 含保证金/强平，属 M2 |
| E3 | [撮合合同](../../../docs/contracts/matching.md) §8 的**八条**订单簿用例全部通过 | 含跨档拆分、自成交、price improvement |
| E4 | 同配置同种子两次运行的**事件摘要哈希相同** | SC-002 |
| E5 | 事件全序键严格递增，KR-006 单调性断言在违反时抛异常并终止 | 事件 Schema §1.1 |
| E6 | 仅凭事件日志可重建全部账户终态；因果链引用完整性检查通过 | SC-006 |
| E7 | 规范序列化产出逐字节确定的 JSONL（键序、无多余空白、`ensure_ascii=false`） | ADR-001 §7 |
| E8 | 核心领域层无 NumPy 等第三方导入 | KR-005；用导入检查测试固化 |
| E9 | 订单簿与账本分支覆盖率 ≥ 90% | NFR-002 |

**不在 M1 退出条件内**：性能门槛（BENCH-001 需要代理才能产生负载，M2 校准）。

## 4. 依赖的实现合同

M1 的每一行代码都应能指到下列某一节。**合同是唯一裁判**——实现与合同不符时以
合同为准，除非能证明合同本身有误（那时先修合同，见 PRD §10）。

| 领域 | 合同 |
|---|---|
| 撮合、定序、成交价、跨档、自成交 | [`matching.md`](../../../docs/contracts/matching.md) |
| 事件类型、字段、因果外键、序列化 | [`event-schema.md`](../../../docs/contracts/event-schema.md) |
| 账户字段、记账、`entry_notional` 更新 | [`margin-and-account.md`](../../../docs/contracts/margin-and-account.md) §1—§2 |
| 期望值 | [`acceptance-vectors.md`](../../../docs/contracts/acceptance-vectors.md) |
| 数值、舍入、序列化 | [ADR-001](../../../docs/adr/001-numeric-and-serialization-contract.md) |
| 事件调度、KR-006 | [ADR-002](../../../docs/adr/002-same-timestamp-event-scheduling.md) |
| 退化状态（空簿、单边簿、无成交） | [`degenerate-states.md`](../../../docs/contracts/degenerate-states.md) §1—§2 |

## 5. 验收方式

```text
pytest tests/                      # 全部通过
python -m market_game_sim.verify --log <run.jsonl>
    → 重建账户终态、校验因果链、校验 C1/C2、输出事件摘要哈希
```

`verify` 是**独立于内核的检查器**：它只读事件日志，不导入 `kernel/` 或 `ledger/`。
若它复用内核代码，就无法证明日志自包含（SC-006 的意义所在）。

## 6. 已知风险

| 风险 | 应对 |
|---|---|
| 验收向量案例 6—9 未在 M1 覆盖，保证金相关缺陷推迟暴露 | 账户字段与状态机在 M1 就位并留有断言桩，M2 只填逻辑 |
| 无代理导致订单流不真实，掩盖某些簿状态 | 属性测试用随机订单流补充（plan §5.2） |
| 合同本身可能仍有洞 | 四轮复审已过，但实现是最终检验；发现洞时先修合同再改代码 |
